"""The OpenWorker tool runner. Everything in this package uses the standard library only
and imports nothing outside the package: it is packed into one file and run inside a
sandbox, in whatever image the sandbox uses."""
