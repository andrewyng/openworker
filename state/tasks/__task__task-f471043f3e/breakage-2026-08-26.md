# Dependency & breakage watch — 2026-08-26

Watch list: ROCm 7.1.0 · llama.cpp 4df29be4f (Vulkan+HIP) · ollama 0.32.14 · torch 2.11.0+rocm7.13.0 (gfx1151 nightly) · transformers 5.5.0 · unsloth (AMD/gfx1151) · LibreChat librechat-dev:latest · OpenWorker (andrewyng) · kernel 7.0.0-29-generic.

Continuation run — first check since 2026-08-24 (breakage-2026-08-24.md). FOCUS.md (rewritten 2026-08-24) puts workstation-stack, the OpenWorker fleet, and openEvolve Phase 3 in play; tags follow. **Three state changes since yesterday: OpenWorker shipped v0.2.0+v0.2.1 with explicitly-recommended security fixes, ollama shipped v0.33.0 (including a Linux packaging fix), and unsloth shipped v0.1.803-beta.** Everything else is stable or unchanged. Ordered by likelihood of biting this box.

---

## ON-FOCUS

### 1. STATE CHANGE — OpenWorker: v0.1.7 → v0.2.0 → v0.2.1 in 24 hours, and the notes say "Updating is recommended"
**ON-FOCUS (OpenWorker fleet — FOCUS item 2, the unattended-fleet question). Likelihood: MEDIUM — this is upstream of the app you run daily, and the delta is explicitly security/approval handling.**

- **v0.2.0, released 2026-08-24 23:53 UTC (commit 44538a7)** — opened in full: Skills as loadable instruction packs, security coworkers (code review / cloud posture / dependency audit), cross-session Memory, auto-approve mode, guided MCP server setup ("connection failures now surface clearly instead of failing silently"), Ark providers, Intel Mac DMG. **Fixed:** scheduled automations now handle DST and weekday rules; "Clone/pull operations respect the approval gate"; Python 3.10 support. **Security hardening (direct quote, opened):** "secrets files are created with private permissions, web fetch connections are pinned, and MCP dependencies were bumped, alongside a broader batch of approval-handling and workspace-trust fixes. **Updating is recommended.**"
  - https://github.com/andrewyng/openworker/releases/tag/v0.2.0 (opened this run)
- **v0.2.1, released 2026-08-25 17:07 UTC (commit 7fc3ee6)** — opened in full: only "Ox Alpha (stealth preview) joins the model picker via OpenRouter — 1M-token context, tool calling". Small on purpose.
  - https://github.com/andrewyng/openworker/releases/tag/v0.2.1 (opened this run)
- First flagged on the list as "v0.1.7, no newer release" on 2026-08-22; **now at v0.2.1.** Neither the v0.2.0 body nor any page I opened lists CVEs for the security items — treat "updating is recommended" as a strong nudge about the approval-handling/workspace-trust fixes specifically, which bear directly on the FOCUS open question "does the fleet survive unattended with no silent failures". If your `~/openworker` tracks upstream, this is the release to diff against.

### 2. STATE CHANGE — ollama: v0.32.15 → **v0.33.0** (you're on 0.32.14, i.e. two behind)
**ON-FOCUS (workstation-stack, FOCUS item 4). Likelihood: LOW-MEDIUM — one entry is a Linux build bug being fixed, the rest are agent-client behavior changes.**

- **v0.33.0, published 2026-08-21 22:52 UTC (commit ebf200f)** — opened in full:
  - Claude Desktop can be configured as a third-party gateway against Ollama.
  - **Prefill caching overhaul:** "Fixed a hang where agent clients that cancel long prefills"; restore points "trustworthy by construction" — a cancelled prefill keeps every restore point it crossed, so retries resume instead of restarting; models with recurrent layers previously "forced a request matching 46k of 47k tokens to reprocess from zero"; disabled Claude Code's token-countdown system message which "broke the KV cache on every request".
  - **"Fixed broken default packaging caused by macOS-specific assumptions affecting Linux/Windows builds"** — a Linux build was literally broken in the default package and is being repaired in this release. If your 0.32.14 package dates from that window, v0.33.0 is the thing that fixes it.
  - MLX dependency update (#17886); DeepSeek Harness launcher npx fallback.
  - https://github.com/ollama/ollama/releases/tag/v0.33.0 (opened this run); latest-tag confirmed via API: tag **v0.33.0**.
- No CVEs, no AMD/HIP-specific entries in the notes I read.

### 3. NEW ACTIONABLE (not a state change, now verified) — the `torch._grouped_mm` MoE-probe SIGSEGV on Strix Halo has a documented two-line workaround, and your exact wheel is the *fixed* one for the other _grouped_mm bug
**ON-FOCUS (workstation-stack / the training path). Likelihood: MEDIUM — only if `import unsloth` or any MoE-probing import runs on this box.**

- **Opened in full this run** (it 404'd last week, so this is the first verified read): Sleeping Robots, "Unsloth Studio on Strix Halo" — `import unsloth` calls `torch._grouped_mm` to probe MoE support; "On ROCm, this probe doesn't raise a Python exception, it SIGSEGVs (exit 139). Setting **`UNSLOTH_MOE_BACKEND=native_torch`** bypasses the probe entirely." Same article (all Strix-Halo/gfx1151-specific):
  - after any `pip install -U unsloth`, re-install torch from the gfx1151 index or check `torch.version.hip` — **reconfirms the CUDA-substitution trap you've hit before** ("can pull CUDA PyTorch from PyPI, silently replacing the ROCm build");
  - bitsandbytes on HIP 7.13 looks for `libbitsandbytes_rocm713.so` but the preview wheel ships 7.1 → set **`BNB_ROCM_VERSION=71`**;
  - "The TheRock nightlies **may cap GPU memory allocation to 64 GB**" on 128 GB unified — budget training sizes accordingly.
  - https://sleepingrobots.com/dreams/unsloth-studio-strix-halo (opened this run)
- **The other `_grouped_mm` class is good news for you:** TheRock #5284 "torch._grouped_mm null HIP kernel crashes training on ROCm 7.12 … **This is fixed in the ROCm 7.13 wheel (torch==2.11.0+rocm7.13.0)**" — that is *exactly* your installed build, so you're on the fixed side of that specific bug (search snippet for the issue; the version match is exact).
  - https://github.com/ROCm/TheRock/issues/5284 (search result, snippet)
- **unsloth itself moved:** latest release is now **v0.1.803-beta, published 2026-08-25** ("Bug Fixes + Auto compaction + LAN Remote Access") — up from v0.1.801-beta on 2026-08-20. I read the tag/summary only, not the full note.
  - https://api.github.com/repos/unslothai/unsloth/releases/latest (opened this run)
- PR #5301 (Strix Halo/Windows ROCm support) remains merged in main per 2026-08-22; no state change re-verified this run.

### 4. STATE CHANGE — llama.cpp: v0.2.0 → **v0.3.0** (2026-08-25), nightly line at **b10621**
**ON-FOCUS (workstation-stack — your inference path is a pinned llama.cpp build). Likelihood: LOW for you (pinned at 4df29be4f) — matters only on rebuild.**

- **v0.3.0, published 2026-08-25 10:22 UTC (commit c1d0e7a)** — opened in full. Body is model-centric: dots3-note multimodal model (new DSA-ISWA KV cache, #27060), MTP support for GLM-4.5-Air (#26534), DeepSeek 4 tensor-split `-sm tensor` (#26490) + multi-sequence rollback fix (#26756), bailingmoe3 DSpark, mamba2 GEMM dispatch, WebP decode, Pillow-accurate mtmd resize. **ggml bumped to v0.22.0 (ggml/1607)** — per the body it "brings … assorted fixes across the CUDA, Metal, SYCL, **Vulkan**, OpenCL and WebGPU backends" (no RDNA/gfx1151-named entry in what I read).
  - Vulkan-relevant in the commit list: `vulkan : added the PAD_REFLECT_1D operation (#26586)` — additive, not a break.
  - Notable CI line: **`ci : Restore ROCm job for Ubuntu (#27399)`** — the prebuilt-ROCm build (DISABLED per 2026-08-20's read of b10519 assets) appears to have come back. I did not verify current asset state; claim limited to the commit line.
  - https://github.com/ggml-org/llama.cpp/releases/tag/v0.3.0 (opened this run)
- **Nightly pointer = b10621** (opened the `nightly-tag.txt` asset directly this run) — up from b10566 in yesterday's v0.2.0 note and b10545 on 2026-08-21.
  - https://github.com/ggml-org/llama.cpp/releases/download/v0.3.0/nightly-tag.txt (opened this run)
- spirv-headers remains the documented Vulkan build requirement (carried from 2026-08-20/21, no new evidence either way). No CMake 4.x/hipcc finding surfaced in this run's searches.

### 5. NO CHANGE — ROCR `AsyncEventsLoop` busy-spin bug family (your torch wheel channel)
**ON-FOCUS (workstation-stack). Still the top gfx1151 runtime risk.**

- **TheRock #7051 — first flagged 2026-08-18, unchanged again:** re-opened via API this run — state=**open**, `status: triage`, assignee adityas-amd, **4 comments, last updated 2026-08-13** (same as 2026-08-24's read). ~3.5 weeks, no movement.
  - https://api.github.com/repos/ROCm/TheRock/issues/7051 (opened this run)
- **ROCm/legacy-rocm-build #6634 (gfx1201 second report, flagged 2026-08-24) — unchanged:** state=open, 0 comments, created 2026-08-20. Same environment (Radeon RX 9070 XT, your kernel family). No fix, no response.
  - https://api.github.com/repos/ROCm/legacy-rocm-build/issues/6634 (opened this run)
- New context from this run's searches (snippets, not individually opened): ROCm/TheRock #2850 "Segfault after ROCM/Torch update — Strix Halo, Arch 6.18.4, assignee huanrwan-amd, status: triage" and ROCm #5853/#5824 (the _grouped_mm/VRAM-access segfault cluster from 2025) remain in search results as the standing gfx1151 crash taxonomy. None show a fix landing.

### 6. NO CHANGE — transformers 5.5.0 (latest still v5.15.1) and the trust_remote_code class
**ON-FOCUS (workstation-stack). Low risk unless you upgrade.**

- Latest re-confirmed via API this run: **v5.15.1, published 2026-08-19 10:50 UTC** — same as 2026-08-22/24 reads. No v5.15.2+.
  - https://api.github.com/repos/huggingface/transformers/releases/latest (opened this run)
- The IBM bulletin that surfaced on 2026-08-24 is now visible in search results with a different, concrete entry than last week's snippet: **CVE-2026-1839** — "HuggingFace Transformers … `Trainer` class … `_load_rng_state()` … `torch.load()` without `weights_only=True` … **affects … when used with PyTorch versions below 2.6 … resolved in version v5.0.0rc3**" (search snippet; I did not open the page). Your stack (transformers 5.5.0, torch 2.11.0) is on the fixed side **on both** axes per that snippet — but it remains snippet-level, flagged unverified like before.
  - https://www.ibm.com/support/pages/security-bulletin-vulnerabilities-found-watson-data-intelligence-3 (search snippet, not opened)

### 7. NO CHANGES FOUND — stated plainly (all ON-FOCUS: workstation-stack)
- **ROCm 7.1.0:** no new release, CVE, or gfx1151-specific finding this run beyond items 5–6. (Searches 1, 2, 4 negative for anything new-and-open.)
- **torch 2.11.0+rocm7.13.0:** no new CUDA-substitution report beyond the standing unsloth trap (item 3); TheRock #5284 snippet positions your exact wheel as the *fixed* wheel for the 7.12 `_grouped_mm` crash (item 3). No new nightly-index version claims — I do not name a "current" nightly I did not read.
- **kernel 7.0.0-29-generic:** no new regression report against your kernel. One snippet worth carrying: the kyuz0 Strix-Halo toolboxes README warns "Kernels **older than 6.18.4** have a bug that causes stability issues on gfx1151" and "**do NOT use `linux-firmware-20251125`** — it breaks ROCm support on Strix Halo" — you predate neither (search snippet; repo README, not opened).
- **LibreChat (librechat-dev:latest):** the `danny-avila/LibreChat` repo itself is confirmed live this run (repo API opened; project description current). Still no release/cve I could pin — as on 2026-08-24, **stale, unverifiable, no new finding**.
  - https://api.github.com/repos/danny-avila/LibreChat (opened this run)

---

## ADJACENT (capped at three)

### A. PyTorch `weights_only` unpickler CVE — snippet only, not opened
**CVE-2026-24747** — "high severity … CVSS **8.8** … a vulnerability in PyTorch's `weights_only` unpickler allows an attacker to craft a malicious checkpoint file (`.pth`) that, when loaded with `torch.load(..., weights_only=True)`, can corrupt memory and potentially lead to arbitrary code execution" — from a CVE aggregator (strobes.co), **search snippet, not opened**, and it does not name affected/fixed versions. If you load untrusted checkpoints with `weights_only=True`, treat as "verify before relying on either side". Related, same snippet pool: **CVE-2026-71281** in **PEFT** (LoRA-GA/CorDA `torch.load` cache deserialization, NHIMG article dated 2026-08-07, snippet) — relevant only if the openEvolve Phase 3 training path ever loads third-party LoRA cache artifacts.
- https://strobes.co/vi/cve/CVE-2026-24747 (search snippet) · https://nhimg.org/articles/peft-unsafe-cache-loading-exposes-ml-pipelines-to-code-execution (search snippet)

### B. Open Ollama issue: containerized Strix Halo memory reporting — snippet only
A popularai.org troubleshooting guide (search snippet) references an **open 2026 Ollama issue filed 2026-06-03**: containerized Strix Halo (Radeon 8060S) ROCm container "recognized … about 113.5 GiB total memory, but only 2.0 GiB available … the same machine with an earlier 0.30.2 [sic] … exposed about 113.8 GiB of 114.0 GiB … **as of August 14, 2026, the issue remains open**". If your ollama runs containerized (it does, via the workstation-stack images) and a `docker compose pull` to v0.33.0 happens later, check `available` memory right after — that regression class would look like a GPU that's "almost out of VRAM" at idle. Unverified: I did not open the guide or the underlying issue.
- https://www.popularai.org/p/ollama-amd-gpu-cpu-rocm-7 (search snippet)

### C. linux-firmware 20251125 breaks Strix Halo ROCm (community report, snippet)
Covered in 7, repeating its practical half here so it is not lost: the kyuz0 toolbox docs (search snippet) instruct Strix Halo users to hold `linux-firmware` away from **20251125** ("breaks ROCm support … instability, crashes, ROCm containers failing to start"; safe: 20240318 or 20260110+). You're on kernel 7.0.0-29 — check what firmware date is actually in the tree before any distro upgrade. Source snippet only, not opened:
- https://raw.githubusercontent.com/kyuz0/amd-strix-halo-toolboxes/main/README.md (search snippet)

---

## Net assessment
Three genuine state changes, all on the "did a newer version ship" axis: **OpenWorker v0.2.0/v0.2.1** (the "Updating is recommended" security line is the one worth acting on, and it speaks directly to the unattended-fleet question in FOCUS), **ollama v0.33.0** (Linux packaging fix + prefill-cancel hang fix), and **unsloth v0.1.803-beta** (no AMD-specific claim made). Plus one *verification win*: last week's 404'ing Strix-Halo unsloth article opened in full this run and gives you a two-flag workaround (`UNSLOTH_MOE_BACKEND=native_torch`, `BNB_ROCM_VERSION=71`) for the `torch._grouped_mm` MoE probe that has cost you SIGSEGV 139s. The two big open risks — the ROCR AsyncEventsLoop spin (both #7051 and #6634 still open, zero movement) and the general gfx1151 segfault cluster — are **unchanged from 2026-08-24**; no fix, no new report, nothing re-reported as new. No new CVE with a directly-opened primary source; the two CVE names above are snapshot-only and should not be treated as confirmed without reading the advisory.

## Budget & sources
**4 of 6 tavily searches used:** (1) Strix Halo/gfx1151 ROCm regression (month); (2) `torch._grouped_mm` segfault (all-time); (3) ollama 0.33.0 ROCm/Vulkan (week); (4) ROCm/torch CVE (month); (5) llama.cpp v0.3.0 build (week).
**Direct opens (outside search budget):** TheRock #7051 API, legacy-rocm-build #6634 API + full body, transformers latest API (full), ollama v0.33.0 release page + latest API, llama.cpp v0.3.0 release page (full) + nightly-tag.txt asset, OpenWorker v0.2.0 + v0.2.1 release pages (full) + latest API, unsloth latest API, danny-avila/LibreChat repo API, sleepingrobots.com unsloth/Strix-Halo article (full).
**Snippet-only (flagged unverified in text):** TheRock #5284, ROCm #5853/#5824, TheRock #2850, IBM CVE-2026-1839 entry, strobes.co CVE-2026-24747, nhimg.org PEFT CVE-2026-71281, popularai.org Ollama Strix-Halo issue ref, kyuz0 toolbox README, lobehub/strix-halo-setup skill page.
