# Dependency & breakage watch — 2026-08-27

Watch list: ROCm 7.1.0 · llama.cpp 4df29be4f (Vulkan+HIP) · ollama 0.32.14 · torch 2.11.0+rocm7.13.0 (gfx1151 nightly) · transformers 5.5.0 · unsloth (AMD/gfx1151) · LibreChat librechat-dev:latest · OpenWorker (andrewyng) · kernel 7.0.0-29-generic.

Continuation run — first check since 2026-08-26 (breakage-2026-08-26.md). FOCUS.md (rewritten 2026-08-24) puts workstation-stack, the OpenWorker fleet, and openEvolve Phase 3 in play; tags follow. **One genuinely new state change: ollama shipped v0.33.1 (2026-08-26 18:09 UTC) on top of v0.33.0. transformers latest moved v5.15.1 → v5.16.1, but that sits at/above the 5.x line you already track (5.5.0), so it is a carry-forward, not a break.** The gfx1151/ROCm and torch-wheel concern set is **unchanged** from 08-26; I could not expand it because both network search tools were down this run (see "Budget & sources" — flagged plainly, not reconstructed).

## ON-FOCUS

### 1. STATE CHANGE — ollama: v0.33.0 → **v0.33.1** (2026-08-26 18:09 UTC, 3 commits)
**ON-FOCUS (workstation-stack — the RoCM Ollama build; FOCUS item 4). Likelihood: LOW.**

- **Opened the release page directly this run.** Changes (verbatim, from https://github.com/ollama/ollama/releases/tag/v0.33.1):
  - `MLX: Qwen3.8 Flash Next support`
  - `cmake: make external compat patches idempotent`
  - `MLX and llama.cpp update`
  - `mlxrunner: add structured output support`
  - `mlxrunner: avoid Metal GPU timeouts when loading models from slow storage`
- Practical read: 0.33.1 is a **3-commit MLX/Metal housekeeping release**, not an ROCm/Linux change. The ROCm artifact is still present in this release — `ollama-linux-amd64-rocm.tar.zst` is listed in the release assets (confirmed via the `ollama/releases/latest` API, which now points at v0.33.1, same URL that held v0.33.0 yesterday). So there is no "the ROCm wheel vanished" surprise here.
- The one item that touches your watch family is **`cmake: make external compat patches idempotent`** — this is a *build-time* fix to how external-compat patches are applied (it is the natural successor to the standing 2026-08-21 watch item "CMake 4.x rejects the hipcc wrapper"). It did not land as a CMake-4.0 breakage; I did not open the patch, so I cannot say whether it *removes* a later CMake 4.x incompatibility. Claim: it is a cmake-compat hardening commit, directionally good-news if you rebuild from source, unverified on the CMake 4.x axis.
- The Linux packaging bug that motivated the whole 0.33.x line was already fixed in **v0.33.0** (2026-08-21, item carried from 08-26). v0.33.1 neither re-breaks nor improves it. If you are still on 0.32.14 (two behind), the meaningful jump is still 0.33.0.
- Net: a newer version shipped, but nothing in 0.33.1 is a new break vector for this box. Re-affirm the standing instruction from 08-26: if a `docker compose pull` to ≥0.33.1 lands, check `available` GPU memory right after (the open 2026 containerized-Strix-Halo VRAM-reporting issue from 08-26 item B remains unresolved).

### 2. CARRY-FORWARD — transformers 5.5.0 sits on/above the 5.x line (latest moved v5.15.1 → v5.16.1)
**ON-FOCUS (workstation-stack). Likelihood: LOW — carry-forward, not a fresh finding.**

- **Opened the release page directly this run.** transformers latest is now **v5.16.1** (2026-08-26 14:50 UTC), up from v5.15.1. I read the full release note (https://github.com/huggingface/transformers/releases/tag/v5.16.1). It adds GLM-5.3-Flash support and two "small patch fixes": `Restore BC for the tensor-parallel API (#48300)` and `Fix kernel commit and repo paths for ESMFold2 (#48186)`.
- Relevance to your watch (transformers 5.5.0 + `trust_remote_code` models): **ESMFold2 is a `trust_remote_code` model path.** The fix #48186 is a carry-forward, not a break, and it is a path/commit fix in the newest release — it does not retroactively change 5.5.0, and it is additive, not a removal. So the trust_remote_code class carries forward unchanged: your 5.5.0 predates both the ESMFold2 and GLM-5 support and is unaffected by either.
- The RCE/trust_remote_code risk framing from 08-24 (the unverified IBM bulletin) **remains unverified**: no directly-opened primary source, no confirmed CVE ID. Still a low-priority rumor, not re-reported as new.
- Net: latest is 5.16.1; your 5.5.0 is far below it, on the same 5.x line, and 5.16.1's changes are not breakers for you. **No action.**

### 3. CARRY-FORWARD (UNCHANGED) — TheRock gfx1151/ROCm segfault & AsyncEventsLoop families
**ON-FOCUS (workstation-stack / the training path). Still the top gfx1151 runtime risk.** No new searches possible this run, so state is reported verbatim from 08-26:
- **TheRock #7051** — state=open, `status: triage`, assignee adityas-amd, 4 comments, last updated 2026-08-13. No fix, no movement since 08-26. (First flagged 2026-08-18.)
- **legacy-rocm-build #6634** — state=open, 0 comments, created 2026-08-20 (gfx1201, same kernel you run). No movement since 08-26.
- **TheRock #5284** carries its own good-news note: your exact wheel `torch==2.11.0+rocm7.13.0` is the *fixed* one for the 7.12 `_grouped_mm` null-HIP-kernel crash. (Snippet-only, as on 08-26.)
- The unsloth MoE-probe workaround set is unchanged: `UNSLOTH_MOE_BACKEND=native_torch`, `BNB_ROCM_VERSION=71`. (Article opened in full on 08-26.)

## ADJACENT (capped at three)

### A. NO CHANGE — OpenWorker, llama.cpp, ROCm, torch nightly, kernel
- **OpenWorker (andrewyng):** latest still **v0.2.1** (2026-08-25 17:07 UTC), confirmed via API. No newer release since 08-26. The v0.2.0 "Updating is recommended" security/approval-handling line is a carry-forward, not a fresh finding.
- **llama.cpp:** latest stable still **v0.3.0** (2026-08-25, c1d0e7a), nightly still **b10621** — no change since 08-26. `ci : Restore ROCm job for Ubuntu (#27399)` carries forward as an unverified ROCm-asset lead. spirv-headers unchanged.
- **ROCm 7.1.0 / torch 2.11.0+rocm7.13.0:** no net-new CUDA-substitution report this run (search tools down). The unsloth-installed-torch trap (08-26 item 3) and TheRock #5284 "fixed wheel" framing carry forward unchanged.
- **kernel 7.0.0-29-generic:** no net-new regression report (search tools down). The kyuz0 "kernels older than 6.18.4" / "do NOT use linux-firmware-20251125" note carries from 08-26, still snippet-only, still unverified.

### B. NO CHANGE — unsloth (gfx1151)
- Latest still **v0.1.803-beta** (2026-08-25, "Bug Fixes + Auto compaction + LAN Remote Access"), confirmed via API. The `cmake: make external compat patches idempotent`-family theme in 0.33.1 does not touch unsloth. No AMD-specific claim in the 0.1.803-beta note I read. Carried unchanged.

### C. NO CHANGE — LibreChat
- **LibreChat librechat-dev:latest:** unverifiable on 08-26, and again this run. No new release/CVE surfaced and no search could be run. Still stale, no finding to report. (Repo API confirmed live on 08-26; no change re-verified here.)

---

## Net assessment
One new release landed since 08-26 and it is **not** a new break vector: **ollama v0.33.1** is a 3-commit MLX/Metal housekeeping release — the ROCm Linux wheel is still present in it, and the only item touching your watch family (`cmake: make external compat patches idempotent`) is a build-time hardening commit, unverified on the CMake-4.x axis. **transformers latest moved v5.15.1 → v5.16.1**, but your 5.5.0 sits below it on the same 5.x line, and 5.16.1's changes (GLM-5.3-Flash, ESMFold2 path fix, TP-restore BC) are addbacks, not breakers — so it is a carry-forward, not a finding. Everything else is unchanged: OpenWorker v0.2.1, llama.cpp v0.3.0/b10621, unsloth v0.1.803-beta, and the top gfx1151 runtime risk (TheRock #7051 and legacy-rocm-build #6634, both open, zero movement) carry forward verbatim.

**Budget limitation, stated plainly:** both network search tools (Tavily, Exa) returned failures this run (Tavily: `getaddrinfo EAI_AGAIN`; Exa: `fetch failed`). I therefore performed **0 net-new searches** and could not confirm any new gfx1151/ROCm/torch-wheel regression since 08-26. The status of those standing items is *not* "confirmed clean" — it is "unchanged from 08-26, unexpanded because no search could run." The version-of-record items above were all verified directly via the GitHub REST API and by opening the release pages, so they stand on primary sources, not memory.

## Budget & sources
**0 of 6 tavily searches used** — Tavily API returned `getaddrinfo EAI_AGAIN` on every attempt (5 attempts across the run), and Exa returned `fetch failed`. No results were reconstructed from memory.

**Direct API opens (version-of-record, outside search budget):**
- `https://api.github.com/repos/ollama/ollama/releases/latest` → **v0.33.1** (2026-08-26 18:09), asset list incl. `ollama-linux-amd64-rocm.tar.zst`
- `https://api.github.com/repos/huggingface/transformers/releases/latest` → **v5.16.1** (2026-08-26 14:50)
- `https://api.github.com/repos/andrewyng/openworker/releases/latest` → **v0.2.1** (2026-08-25 17:07)
- `https://api.github.com/repos/ggml-org/llama.cpp/releases/latest` → **v0.3.0** (2026-08-25 10:22)
- `https://api.github.com/repos/unslothai/unsloth/releases/latest` → **v0.1.803-beta** (2026-08-25 13:12)

**Release pages opened (full text):**
- ollama v0.33.1 release page (changes listed verbatim in item 1)
- transformers v5.16.1 release page (GLM-5.3-Flash + #48186/#48300 in item 2)

**Snippet-only / unverified, carried from 08-26 (no new verification this run):** TheRock #7051, #6634, #5284; unsloth Strix-Halo workaround article (opened in full 08-26); IBM transformers-RCE bulletin; kyuz0 firmware/kernel note; strobes.co/PEFT CVEs.
