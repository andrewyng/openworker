# Dependency & breakage watch — 2026-09-03

Watch list: ROCm 7.1.0 · llama.cpp 4df29be4f (Vulkan+HIP) · ollama 0.32.14 · torch 2.11.0+rocm7.13.0 (gfx1151 nightly) · transformers 5.5.0 · unsloth (AMD/gfx1151) · LibreChat librechat-dev:latest · OpenWorker (andrewyng) · kernel 7.0.0-29-generic.

Continuation run — first check since 2026-09-02 (breakage-2026-09-02.md). FOCUS.md (rewritten 2026-08-31) puts workstation-stack, the OpenWorker fleet, and the parked openEvolve/Phase 3 work in play; tags follow. **One net-new, and it is the one AMD/gfx1151 item whose version-of-record actually moved:** **unsloth shipped v0.1.806-beta** (published 2026-09-02 12:48 UTC) — from v0.1.804-beta. Its release body leans hard on AMD/ROCm ("Strengthened AMD/ROCm detection, installation, and GPU compatibility," "AMD installs choose the best build across Windows and Linux, with BF16 on more GPUs"), so it is on-FOCUS and worth a real read rather than a pass. The running posture on every other item is **unchanged** from 09-02.

One nuance worth carrying: **the unsloth move is a *Desktop-app* release, not the torch/ROCm training path** — same as the v0.1.804-beta "AMD model loading crashes fixed" note. The 0.1.80x-beta line is the Desktop app; the *training*-path trap ("fresh `pip install -U unsloth` pulls a CUDA-torch wheel onto this box") is unchanged, and the reinstall guardrail still applies. It does **not** change the CUDA-substitution posture.

**Budget, stated plainly:** `tavily-tavily_search` was **UP** this run (the `getaddrinfo EAI_AGAIN` block that hit 08-26 → 09-02 did not recur); 4 of 4 queries returned, within the 6-search budget. MCP **context7 was also up** this run (resolved + queried) — first time it has been exercised since the 09-02 run reported it failing (`TypeError: fetch failed`), and I got a live read of the unsloth training/RDNA guidance. Priority version-of-record / state facts were verified **directly via the GitHub REST API** (and the unsloth release body opened here). No search result was reconstructed from memory.

---

## ON-FOCUS

### 1. MOVED — unsloth v0.1.804-beta → **v0.1.806-beta** (2026-09-02), AMD/ROCm-heavy
**ON-FOCUS (workstation-stack / the AMD/gfx1151 training-and-serving path — FOCUS item 4). Likelihood: LOW, but net-new and on the exact AMD item this watch prioritises.**

- **opened `unslothai/unsloth/releases/latest` API this run** → `tag_name: v0.1.806-beta`, published 2026-09-02T12:48:20Z, author `danielhanchen` (net-new since 09-02; 09-02's value was v0.1.804-beta).
- **What's in v0.1.806-beta (verbatim-sourced from the release body opened this run):**
  - "Run Qwen3.8-Flash-Next and GLM-5.3-Flash **up to 2x faster with MTP**." (feature-additive)
  - "Also our new release includes **170+ training, chat, hardware, and performance improvements**."
  - **Highlights (verbatim):** *Smoother model loading*; *Safer chat edits*; *New local media APIs* (video/audio/MLX); *New audio support* (MiniMax-Music3, Higgs, MOSS); *Improved multi-GPU planning, memory fitting, split-model training*; **"Strengthened AMD/ROCm detection, installation, and GPU compatibility"**; *Upgraded MCP, Deep Research, OAuth, and agent tool reliability.*
  - **Training + hardware (verbatim):** *"Train larger models across multiple GPUs with automatic placement. **AMD installs choose the best build across Windows and Linux, with BF16 on more GPUs.** Export GLM-5.3 MLX fine-tunes to GGUF. Choose custom GGUF shard sizes…"*
- **Relevance to THIS box — a refinement, not a downgrade, and not a fix to the trap:**
  - This is the **Unsloth Desktop app** release (assets: `Unsloth-Desktop-Linux.AppImage`, `Unsloth-Desktop-Ubuntu.deb`, `macOS.dmg`, `windows.exe`). It is **not** the torch/ROCm training library that pulls a CUDA-torch wheel onto this box.
  - The **CUDA-substitution trap is unchanged** (carried from 08-26, still live): if you `pip install -U unsloth` again it can pull a CUDA-torch wheel. **The guardrail is unchanged** — re-install torch from the gfx1151 nightly index, set `BNB_ROCM_VERSION=71` if bitsandbytes is pulled, set `UNSLOTH_MOE_BACKEND=native_torch` if an MoE-probe import runs.
  - Context7 corroborates that unsloth's **RDNA detection now covers gfx1151** (the `is_rdna()` helper lists `gfx1150/1151/1152` as "RDNA3.5 (Strix Point / Strix Halo)"), and the Gemma3 loader has a gfx1151-capable ROCm/HIP compile-disable workaround. So the "Strengthened AMD/ROCm detection" bullet is credible, not marketing fluff — but it's about **Desktop-app model loading**, not your host's torch ROCm runtime.
- **Net:** a newer unsloth shipped and it is genuinely AMD-forward, **but** it is the Desktop app, and it does **not** alter the training-path CUDA-substitution posture. **Continue watching**; the 09-02 reinstall guardrail stands.

### 2. TOP RISK (UNCHANGED) — gfx1151 AsyncEventsLoop 100%-CPU-spin, still `status: triage`
**ON-FOCUS (workstation-stack / the training path — FOCUS item 4). Likelihood: HIGH (top gfx1151 runtime risk), status: unchanged from 09-02.**

- **TheRock #7051 — unchanged** (re-verified via API this run): state=**open**, `status: triage`, assignee **adityas-amd**, **5 comments**, created 2026-08-01, last updated **2026-09-01T22:28:04Z** (was 09-01T22:28:04Z in 09-02; no newer comment since). The 5th comment by leapdragon (opened in full 09-02) is still the latest.
  - https://api.github.com/repos/ROCm/TheRock/issues/7051
  - Verbatim title, unchanged: *"[Bug] gfx1151: bundled ROCR 1.21 AsyncEventsLoop busy-spins one full core permanently after any GPU op (nightly AND 7.14 production wheels) — release-channel ROCR 1.18 clean on same kernel."*
  - The 09-02 findings carry verbatim: the Navi 21 (gfx1030) repro, the **class-2 `InterruptSignal::WaitRelaxed` livelock** on the HIP service thread, the RCCL/multi-GPU angle, the four `HSA_*`/hipcc knobs leapdragon measured as **ineffective** for the multi-GPU case, and the **bounded-backoff patch under AMD's hands — not merged, triage still open**.
  - **Relevance to THIS box (carried, unchanged):** the issue's own reproduced matrix puts the **7.13.0 nightly wheel inside the affected 1.21 family**, and your host runs **torch 2.11.0+rocm7.13.0 from the gfx1151 nightly index** — that same 7.13.0-nightly family. **Recommended 30-second check** (not done by me, no box access): `strings` on your bundled `libhsa-runtime64.so` → `1.21.*` = affected (the 1.18 `LD_PRELOAD` shim from isaac-ranger's #7051 comments is the mitigation), `1.18` = the clean release-channel runtime. The stub count is **not stable** (19/15/10 across patchlevels) — regenerate from your own two runtimes, don't copy anyone's "fifteen."
- **legacy-rocm-build #6522 — unchanged** (re-verified via API this run): state=**open**, `status: triage`, assignee **amd-nicknick**, **3 comments**, created 2026-07-26, last updated **2026-08-10T19:56:28Z** (verified in full this run; verbatim title, no change). Still the second, independent open report of the gfx1151 AsyncEventsLoop spin; no fix.
  - https://api.github.com/repos/ROCm/legacy-rocm-build/issues/6522
- **Net for #2:** no fix, no movement on either issue. Priority of watch: it will move out of triage before it gets a fix.

### 3. CARRY-FORWARD (UNCHANGED) — torch/ROCm nightly: the CUDA-substitution trap
**ON-FOCUS (workstation-stack / the training path). Unchanged; no net-new report discovered.**

- **torch 2.11.0+rocm7.13.0 unchanged on the host.** Direct-API + Tavily read: the ROCm install ecosystem is on **stable 2.12.0 / nightly 2.13.0**, 2.11.0 still listed as installable — your 2.11.0+rocm7.13.0 predates none of that in a breaking way; it's simply a couple of minors behind. (opened `rocm.docs.amd.com/projects/ai-ecosystem/.../pytorch/install.html` — confirms 2.12.0 + 2.11.0 + 2.10.0 Docker images and pip path; "Known issues" section present but not opened in full — no net-new host break)
- **The standing trap unchanged** (from 08-26, still live): a fresh `pip install -U unsloth` can pull a CUDA-torch wheel. Guardrail unchanged (item 1 / item 2 above).
- **TheRock #5284** — your exact wheel `torch==2.11.0+rocm7.13.0` is the *fixed* one for the 7.12 `_grouped_mm` null-HIP-kernel crash. (Snippet-only, unchanged; not re-opened, no reason to.)

### 4. CARRY-FORWARD (UNCHANGED) — llama.cpp build-dependency (spirv-headers) + Vulkan/HIP
**ON-FOCUS (workstation-stack — the Vulkan+HIP builds; FOCUS item 4). Unchanged; no net-new.**

- **Latest stable still v0.3.0** (published 2026-08-25, commit `c1d0e7a`, nightly **b10621** per `nightly-tag.txt` asset). Unchanged.
- **No net-new build break.** The Tavily search surfaced a long-standing AUR/weekly-report thread: "[BUG] [AMD GPU] [3RD PARTY] Compile bug: ROCm 7.2 + rocwmma" (ambiguous partial specializations in rocwmma headers triggered by LLVM 21+; 6.4 and 7.1 unaffected) — that is an **existing** fragility, not a new 2026-09 development, and it touches the ROCm 7.2 rocwmma path, not this box's 7.1.0/Vulkan build. The **`spirv-headers` Vulkan requirement** is still documented in `build.md` (`sudo apt-get install libvulkan-dev glslc spirv-headers`). **No net-new CMake-4.x/hipcc regression** (only the same historical `-famd-opt` / rocwmma AUR threads). **No action.**

### 5. CARRY-FORWARD (UNCHANGED) — ollama, OpenWorker, transformers (version-of-record, all via GitHub API)
**ON-FOCUS (workstation-stack). Unchanged.**

- **ollama v0.33.2** (published 2026-08-27 20:31) — unchanged. **The ROCm artifact is still shipped**: `ollama-linux-amd64-rocm.tar.zst` present in the release asset list this run. (opened `releases/latest` API this run)
- **OpenWorker (andrewyng) v0.2.1** (published 2026-08-25 17:07) — unchanged; the v0.2.0 "Updating is recommended" security/approval-handling line carries forward. (opened `releases/latest` API this run)
- **transformers v5.16.1** (published 2026-08-26 14:50) — unchanged. The 5.16.1 addbacks (GLM-5.3-Flash #48342, TP-restore BC #48300, ESMFold2 path fix #48186) do not affect your 5.5.0. **The v5.6.0 "release" claim is still a false lead** (`releases/tag/v5.6.0` → HTTP 404; `releases/latest` = v5.16.1). (opened `releases/latest` API this run)

### 6. NO CHANGE — LibreChat
**ON-FOCUS-adjacent / workstation-stack. Unverifiable via MCP tools this run.** LibreChat librechat-dev:latest — no release/CVE could be surfaced and none re-verified (the MCP tools that could check it are search/API only; LibreChat image/`dev` state isn't a simple release tag). No net-new, no finding, as on 08-26/27/28/31/09-01/09-02. Repo confirmed live 08-26; not re-verified — no reason to, absent a new lead. Stale, no action.

---

## ADJACENT (capped at three)

### A. NO CHANGE — Linux 7.1-rc7 gfx1151 memory-op hang
**Likelihood of breaking THIS box: LOW (not on the affected kernel line).**

- Unchanged from 09-02: the 7.1-rc7 Strix Halo memory-op regression lives on the **7.1-rc kernel series**, and this box runs **7.0.0-29-generic** (the 7.0 release line), so the offending patches (`amdgpu_hmm.c`/`gfxhub_v12_0.c`/`gmc_v12_0.c`/`amdgpu_gart.c`) are not in your kernel. Carried only as Strix-Halo kernel-regression awareness. **If/when the box is ever moved onto 7.1, watch this one first.**

### B. NO CHANGE — Ubuntu 26.04 LTS officially lists gfx1151 as supported ROCm hardware
**Likelihood of breaking THIS box: LOW (informational; not a host change).**

- The Ubuntu 26.04 LTS "changes since 25.10" notes (surfaced via Tavily) now list **`gfx1151` — Strix Halo / Ryzen AI MAX 300 Series (8040S/8050S/8060S) — as `YES` in the ROCm CI-support table**, and point at "ROCm 7.1.0 release notes." This is the upstream ecosystem catching up to Strix Halo officially; **it is not a host change** — your box stays on ROCm 7.1.0/7.13.0 and needs no action. Only relevant if/when you move onto 26.04 LTS. (note / Ubuntu release-notes, snippet only)

### C. ADJACENT (informational) — ROCm Core SDK 10.0.0 / TheRock as the release system
- The ecosystem direction carries forward, unchanged: **ROCm Core SDK 10.0.0** states "Since ROCm 7.14, ROCm uses TheRock as its build and release system," and the gfx1151 torch wheels live on the TheRock/`rocm.nightlies.amd.com` nightly index. **This is the upstream migration direction, not a host change** — your box stays on ROCm 7.1.0/7.13.0 and requires **no action** from it. Only relevant if the box is ever upgraded onto 7.14+. (carried context, snippet only)

---

## Net assessment
**The only net-new version move is unsloth: v0.1.804-beta → v0.1.806-beta (2026-09-02 12:48 UTC), and it is the Desktop-app release, not the torch training path.** Its body is genuinely AMD-forward ("Strengthened AMD/ROCm detection, installation, and GPU compatibility"; "AMD installs choose the best build across Windows and Linux, with BF16 on more GPUs") — and context7 confirms unsloth's `is_rdna()` now lists **gfx1151** (Strix Halo) as supported — but this is **Desktop-app model loading, not your host's torch ROCm runtime**, so **the CUDA-substitution trap and the 09-02 reinstall guardrail are unchanged.** **Everything else is unchanged from 09-02:** TheRock #7051 (open/triage/5-comments/last-2026-09-01) and legacy-rocm-build #6522 (open/triage/3-comments/last-2026-08-10) remain the top gfx1151 runtime risk with **no merge, no movement**; torch 2.11.0+rocm7.13.0 is unchanged on the host (ecosystem on stable 2.12.0/nightly 2.13.0); ollama v0.33.2 (ROCm wheel still shipped); llama.cpp v0.3.0/b10621 (spirv-headers requirement still documented, no net-new CMake-4.x/hipcc break — the rocwmma compile-bug lead is an existing 7.2-line fragility); OpenWorker v0.2.1; transformers v5.16.1 (the v5.6.0 claim is a false lead, tag 404s); LibreChat unverifiable/no change. Also carried: the 7.1-rc7 gfx1151 memory-op hang (off-box), and a low-likelihood informational note that **Ubuntu 26.04 LTS now officially lists gfx1151 as supported**. **No net-new break on this machine; the one movement is unsloth moving on the AMD item, without altering the torch-substitution posture.**

**Budget, stated plainly:** `tavily-tavily_search` was **UP** this run (the `getaddrinfo EAI_AGAIN` block that hit 08-26 → 09-02 did not recur); **4 of 4 queries used** (within 6-cap). MCP **context7 was also up** this run and was exercised (first live context7 read since the 09-02 run reported it failing `TypeError: fetch failed`). Priority version-of-record / state facts were verified **directly via the GitHub REST API** (and the unsloth release body opened here). No search result was reconstructed from memory.

## Budget & sources
**Tavily: 4 used (within 6-cap)** — up after 8 consecutive (08-26→09-02) failures. Queries: gfx1151 Strix Halo segfault regression; PyTorch 2.11 ROCm CUDA-substitution; llama.cpp build-error spirv-headers/CMake-4.x/ROCm; gfx1151 ROCm driver bug (Sept 2026). Discovered leads: Ubuntu 26.04 LTS gfx1151 support row; rocwmma ROCm-7.2 compile-bug (existing, not net-new); NixOS/Strix-Halo ROCm env-var notes.

**Direct GitHub API / release-page opens (version-of-record / state, outside the search budget):**
- `https://api.github.com/repos/unslothai/unsloth/releases/latest` → **v0.1.806-beta** (published 2026-09-02T12:48:20Z, author danielhanchen) — **MOVED** (was v0.1.804-beta)
- `https://api.github.com/repos/ROCm/TheRock/issues/7051` → open, triage, adityas-amd, 5 comments, updated 2026-09-01T22:28:04Z — unchanged
- `https://api.github.com/repos/ROCm/legacy-rocm-build/issues/6522` → open, triage, amd-nicknick, 3 comments, updated 2026-08-10 — unchanged
- `https://api.github.com/repos/ollama/ollama/releases/latest` → v0.33.2; `ollama-linux-amd64-rocm.tar.zst` still present — unchanged
- `https://api.github.com/repos/ggml-org/llama.cpp/releases/latest` → v0.3.0 (c1d0e7a); nightly b10621 — unchanged
- `https://api.github.com/repos/andrewyng/openworker/releases/latest` → v0.2.1 — unchanged
- `https://api.github.com/repos/huggingface/transformers/releases/latest` → v5.16.1 — unchanged (`releases/tag/v5.6.0` → 404, false lead discarded)

**context7 (up this run, opened):**
- `/unslothai/unsloth` — `is_rdna()` explicitly supports **gfx1151** (Strix Halo); Gemma3 loader has a gfx1151-capable ROCm/HIP `UNSLOTH_COMPILE_DISABLE` workaround; unsloth_zoo version floor `>= 2026.8.15` with the standard forced-reinstall remedy.

**Opened in full (adversarial / lead, outside the search budget):**
- `https://rocm.docs.amd.com/projects/ai-ecosystem/en/latest/frameworks/pytorch/install.html` → ecosystem torch/ROCm stable 2.12.0 / nightly 2.13.0, 2.11.0 still installable (not a host change)

**Snippet-only / unverified, carried from 08-26 → 09-03 (no new verification this run):** TheRock #5284 "fixed wheel" framing; legacy-rocm-build #5853 / kyuz0 toolbox segfault lineage; 7.1-rc7 gfx1151 memory-op hang; trust_remote_code / RCE bulletin; strobes.co CVEs; strixhalo.wiki note.
