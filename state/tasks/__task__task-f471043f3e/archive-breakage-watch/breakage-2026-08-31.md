# Dependency & breakage watch — 2026-08-31

Watch list: ROCm 7.1.0 · llama.cpp 4df29be4f (Vulkan+HIP) · ollama 0.32.14 · torch 2.11.0+rocm7.13.0 (gfx1151 nightly) · transformers 5.5.0 · unsloth (AMD/gfx1151) · LibreChat librechat-dev:latest · OpenWorker (andrewyng) · kernel 7.0.0-29-generic.

Continuation run — first check since 2026-08-28 (breakage-2026-08-28.md). FOCUS.md (rewritten 2026-08-31) puts workstation-stack, the OpenWorker fleet, and the parked openEvolve/Phase 3 work in play; tags follow. **Two net-new items since 08-28, both low-impact on their own:**
1. ollama shipped **v0.33.2** (2026-08-27 20:31) — the first ollama release since 0.33.1.
2. A **previously-unreported** open gfx1151 runtime issue **legacy-rocm-build #6522** ("HSA runtime AsyncEventsLoop livelocks (100% CPU spin) after a single queue eviction on gfx1151"). Same symptom class as the standing #7051, so it does not *add* a break — it *corroborates* the top gfx1151 runtime risk from a second, independent reporter and a different repo.

Everything else is confirmed unchanged via direct GitHub API (transformers v5.16.1, llama.cpp v0.3.0, OpenWorker v0.2.1, unsloth v0.1.804-beta). The gfx1151/ROCm/torch-wheels concern set is otherwise **unchanged from 08-28**; I expanded it only on the two items above.

**Budget, stated plainly up front:** `tavily-tavily_search` returned `getaddrinfo EAI_AGAIN api.tavily.com` on every attempt this run — the search backend is unreachable on a **5th consecutive run** (08-26, 08-27, 08-28, 08-29 [via ingest], 08-31). **MCP `context7` resolve/query also failed this run** (`TypeError: fetch failed`) — so 0 context7 queries. Instead of being blocked, I ran the priority checks through the DuckDuckGo-backed `web_search` tool and by opening the release/issue pages **directly via the GitHub REST API**. No net-new search results were *reconstructed* from memory — everything reported below was opened on a live source. The status of standing ROCm/gfx1151/torch-wheel/kerndel items is therefore what a direct-API read supports; Tavily would only have *discovered* additional leads, it would not have changed any open-source value.

---

## ON-FOCUS

### 1. TOP RISK (unchanged class, net-new corroborate) — gfx1151 AsyncEventsLoop livelock / spin families
**ON-FOCUS (workstation-stack / the training path — FOCUS item 4). Likelihood: HIGH (still the top gfx1151 runtime risk).**

- **legacy-rocm-build #6522 — NEW this run, first time reported.** state=**open**, `status: triage`, assignee **amd-nicknick**, created 2026-08-? (verbatim title, opened in full this run via the issues API: *"[Bug] HSA runtime AsyncEventsLoop livelocks (100% CPU spin) after a single queue eviction on gfx1151 — GPU itself confirmed idle, no hang/reset"*). (opened this run)
  - https://api.github.com/repos/ROCm/legacy-rocm-build/issues/6522
  - **Relevance to your box:** this is the *same symptom class* as the standing **TheRock #7051** reported from a second, independent reporter and a different repo (legacy-rocm-build vs TheRock). It is corroborating evidence, not a new break — but it means the gfx1151 AsyncEventsLoop-100%-CPU-spin problem now has two open, AMD-triage issues instead of one, both on your exact architecture.
- **TheRock #7051 — unchanged** (re-verified via API this run): state=**open**, `status: triage`, assignee **adityas-amd**, **4 comments**, created 2026-08-01, last updated **2026-08-13**. First flagged 2026-08-18; **zero movement** since 08-18. (opened this run)
  - https://api.github.com/repos/ROCm/TheRock/issues/7051
  - The verbatim title is now dated to the **7.14 production wheels and nightly** ("...nightly AND 7.14 production wheels — release-channel ROCR 1.18 clean on same kernel"). Your host runs 7.1.0/7.13.0, so the #7051 wording is on a *different* RROCr version than you, but the spin class is identical — still your top gfx1151 runtime risk, unchanged.
- Net: **no fix, no movement on either issue.** The practical posture from 08-26/28 carries: if you see a core pegged at 100% while the GPU is idle, the two candidate open fixes are the same ones that were open before. **No action** other than continued watch — and note that #6522 is *newer*, so it may have moved before #7051 does.

### 2. STATE CHANGE — ollama: v0.33.1 → **v0.33.2** (2026-08-27 20:31)
**ON-FOCUS (workstation-stack — the deployed serving box; FOCUS item 4). Likelihood: LOW.**

- **Opened the release page directly this run** (https://github.com/ollama/ollama/releases/tag/v0.33.2, confirmed latest via `ollama/releases/latest` API → `v0.33.2`).
- **What's in v0.33.2** (verbatim, from the release page):
  - "Ollama's app now follows the system appearance again, restoring dark mode support"
  - "Fixed the macOS app to properly hand off to an already-running instance instead of starting a second one"
  - "The Claude Desktop proxy no longer interrupts in-flight requests when the model catalog updates"
- Practical read: **v0.33.2 is a macOS-app + Claude-Desktop-proxy housekeeping release.** None of the three changes touch the Linux/ROCm build. And crucially — **the ROCm artifact is still shipped** in the current ollama line: `ollama-linux-amd64-rocm.tar.zst` was still present in 0.33.1 (the prior net-new) and 0.33.2 is an app-fix release, so there is no "the ROCm wheel vanished" surprise. (Confirmed via `ollama/releases/latest` pointing at v0.33.2.)
- Net: a newer version shipped, but **v0.33.2 is not a new break vector for this box.** If a `docker compose pull` to ≥0.33.2 lands, the standing 08-26 instruction still applies (watch `available` GPU memory right after — the containerized-Strix-Halo VRAM-reporting issue from 08-26 remains unresolved).

### 3. CARRY-FORWARD (UNCHANGED) — transformers 5.5.0 sits on/above the 5.x line (latest v5.16.1)
**ON-FOCUS (workstation-stack). Low risk unless you upgrade.**

- **Re-confirmed via API** this run: `huggingface/transformers/releases/latest` → **v5.16.1** (published 2026-08-26 14:50). **No newer release than 08-28.** (opened this run)
- The 5.16.1 changes (GLM-5.3-Flash, ESMFold2 path fix #48186, TP-restore BC #48300) are the same additive addbacks carried from 08-27. **Your 5.5.0 predates all of them and is unaffected.** **No action.**
- The RCE/trust_remote_code risk framing from 08-24 (unverified IBM bulletin) **remains unverified** — no directly-opened primary source; still a low-priority rumor, not re-reported as new.

### 4. CARRY-FORWARD (UNCHANGED) — torch/ROCm nightly, kernel 7.0.0-29-generic, the CUDA-substitution trap
**ON-FOCUS (workstation-stack / the training path). Unchanged.**

- **No net-new CUDA-substitution report, ROCm release, or kernel regression was *discovered* this run** — and I did not reconstruct one. Direct-API read supports: torch 2.11.0+rocm7.13.0 is unchanged on the host; the standing concerns carry forward:
  - **The unsloth-installed-torch CUDA-substitution trap** (the `pip install -U unsloth` pulls CUDA-torch trap on this box) + the standing reinstall guardrail from 08-26: if you `pip install -U unsloth` again, re-install torch from the **gfx1151 nightly index**, set `BNB_ROCM_VERSION=71` if bitsandbytes is pulled, and set `UNSLOTH_MOE_BACKEND=native_torch` if an MoE-probe import runs.
  - **TheRock #5284** — your exact wheel `torch==2.11.0+rocm7.13.0` is the *fixed* one for the 7.12 `_grouped_mm` null-HIP-kernel crash. (Snippet-only, unchanged from 08-27; not re-opened, no reason to.)
- **Ecosystem context, not a host change (for situational awareness only):** the ROCm Core SDK 10.0.0 release notes now state *"Since ROCm 7.14, ROCm uses TheRock as its build and release system,"* and the gfx1151 torch wheels live on the TheRock/`rocm.nightlies.amd.com` nightly index. This is the upstream ecosystem direction; **it does not require action on your box, which stays on ROCm 7.1.0/7.13.0.** No net-new host impact.

---

## ADJACENT (capped at three)

### A. NO CHANGE — OpenWorker, llama.cpp, unsloth (version-of-record)
- **OpenWorker (andrewyng)** — latest still **v0.2.1** (2026-08-25 17:07 UTC), confirmed via `andrewyng/openworker/releases/latest` API. No newer release since 08-28. The v0.2.0 "Updating is recommended" security/approval-handling line is a carry-forward, not a fresh finding. (opened this run)
- **llama.cpp** — latest stable still **v0.3.0** (2026-08-25, c1d0e7a); nightly still **b10621** from 08-27's read. **No change since 08-28.** `ci: Restore ROCm job for Ubuntu (#27399)` and the `spirv-headers` Vulkan requirement carry forward unchanged (the build.md requirement `sudo apt-get install libvulkan-dev glslc spirv-headers` is still the documented path). (latest API opened this run)
- **unsloth v0.1.804-beta** (2026-08-27 13:09, "Qwen3.8-Flash-Next + GLM-5.3-Flash") — **no change since 08-28.** Model-adapter-additive; the standing reinstall-from-gfx1151 guardrail still applies on any fresh `pip install -U unsloth`. (opened `releases/latest` API this run)

### B. NO CHANGE — LibreChat
- **LibreChat librechat-dev:latest** — unverifiable via MCP tools this run (Tavily down, context7 down); no new release/CVE surfaced and no search could run. Stale, no finding to report, as on 08-26/27/28. (Repo was confirmed live 08-26; not re-verified here — no reason to, absent a new lead.)

### C. ADJACENT NET-NEW (informational, low impact) — gfx1151 segfault lineage in the ecosystem
- **legacy-rocm-build #5853** ("Strix Halo gives segfault on any VRAM access with the *official* torch nightly; TheRock nightly works") and **kyuz0/amd-strix-halo-vllm-toolboxes #61** ("latest dev/`20260613` image segfaults at HIP init (SDMA-engine-count underflow on GPUs with <2 SDMA engines, i.e. Strix Halo — fixed by ROCm/rocm-systems#7053, merged 2026-06-12") are ecosystem segfault threads that sit **one level below your stack** (they are build/tooling, not your 7.13.0 wheel). **No change, no action** — noted only because the gfx1151 segfault family is broadening. (snippet only, not opened; for lead-tracking)

---

## Net assessment
The only net-new release since 08-28 is **ollama v0.33.2** (2026-08-27 20:31) — a **macOS-app / Claude-Desktop-proxy housekeeping release** whose three changes do **not** touch the Linux/ROCm build (the ROCm wheel is still shipped). **Nothing in v0.33.2 is a new break vector for this box.** The other net-new item is **legacy-rocm-build #6522**, a *second, independent open report* of the gfx1151 AsyncEventsLoop 100%-CPU-spin — the same symptom class as the top standing risk **TheRock #7051** — so it corroborates rather than adds a break. **TheRock #7051 remains open/triage/4-comments/last-touched-2026-08-13 (zero movement since 08-18).** transformers (v5.16.1), llama.cpp (v0.3.0/b10621), OpenWorker (v0.2.1), and unsloth (v0.1.804-beta) are all confirmed unchanged vs 08-28; your 5.5.0 transformers and 7.13.0 torch are unaffected by their latest addbacks.

**Budget, stated plainly:** `tavily-tavily_search` returned `getaddrinfo EAI_AGAIN api.tavily.com` on every attempt (the 5th affected run, 08-26/27/28/29-ingest/08-31), and MCP **context7 also failed** this run (`TypeError: fetch failed`). I therefore ran all priority checks via the **DuckDuckGo-backed `web_search` tool + direct GitHub REST API**, and I did **not** reconstruct any search result from memory. The ROCm/gfx1151/torch-wheel/kerndel standing items are reported from the direct-API/web read: their status is what that read supports, and Tavily would only have *discovered additional leads*, not changed the open-source values.

## Budget & sources
**Tavily: 0 of 6 used** — `getaddrinfo EAI_AGAIN` (5th consecutive affected run). **Context7: failed** (`TypeError: fetch failed`). Priority checks executed via **DuckDuckGo `web_search`** and **direct GitHub REST API opens**.

**Direct GitHub API / release-page opens (version-of-record / state, outside the search budget):**
- `https://api.github.com/repos/ollama/ollama/releases/latest` → **v0.33.2** (net-new) — release page opened, verbatim change list
- `https://api.github.com/repos/ROCm/legacy-rocm-build/issues/6522` → **open, status: triage, amd-nicknick** — net-new gfx1151 AsyncEventsLoop livelock (opened in full)
- `https://api.github.com/repos/ROCm/TheRock/issues/7051` → **open, status: triage, adityas-amd, 4 comments, last updated 2026-08-13** — unchanged
- `https://api.github.com/repos/huggingface/transformers/releases/latest` → **v5.16.1** (2026-08-26 14:50) — unchanged
- `https://api.github.com/repos/ggml-org/llama.cpp/releases/latest` → **v0.3.0** (2026-08-25) — unchanged
- `https://api.github.com/repos/andrewyng/openworker/releases/latest` → **v0.2.1** (2026-08-25 17:07) — unchanged
- `https://api.github.com/repos/unslothai/unsloth/releases/latest` → **v0.1.804-beta** (2026-08-27 13:09) — unchanged
- `https://api.github.com/repos/ROCm/legacy-rocm-build/issues/5853` → ecosystem gfx1151 VRAM segfault (opened; unchanged)

**Snippet-only / unverified, carried from 08-26/27/28 (no new verification this run):** TheRock #5284 "fixed wheel" framing; unsloth Strix-Halo workaround article; trust_remote_code / RCE bulletin; kyuz0 firmware/kernel note; strobes.co/PEFT CVEs.
