# Dependency & breakage watch — 2026-08-22

Watch list: ROCm 7.1.0 · llama.cpp 4df29be4f (Vulkan+HIP) · ollama 0.32.14 · torch 2.11.0+rocm7.13.0 (gfx1151 nightly) · transformers 5.5.0 · unsloth (AMD/gfx1151) · LibreChat librechat-dev:latest · OpenWorker · kernel 7.0.0-29-generic.

Continuation run. Carried state from breakage-2026-08-21.md. Two state changes since yesterday and one genuinely new release; everything else unchanged. Ordered by likelihood of breaking this Strix Halo (gfx1151) machine.

---

## 1. NEW — llama.cpp ships its first semver STABLE release, v0.2.0
**Likelihood: LOW today (you pin commit 4df29be4f), but it reorganizes the release line you track.**
- **v0.2.0, published 2026-08-21 18:32 UTC** (commit bb4caa754). llama.cpp introduces dual versioning: `vX.Y.Z` = "stable, slower release cadence, recommended for downstream distributions", `b[NUM]` = "bleeding edge… could be less stable" (release notes, read in full).
  - https://github.com/ggml-org/llama.cpp/releases/tag/v0.2.0 (opened; full changelog since v0.1.2)
  - https://github.com/ggml-org/ggml/discussions/1579 (referenced by the release notes for the versioning policy; not opened)
- **Vulkan-path changes are in this changelog** — i.e. in your build surface if you ever rebuild from the Vulkan path rather than the pinned commit:
  - `vulkan: FA MMQ should use fp32 for Q quantization calculations (#27413)`
  - `vulkan: dequant q8_0 KV once in coopmat1 (#25494)` — this is the cooperative-matrix KV dequant path that matters on RDNA iGPUs
  - `vulkan: add null checks in ggml_vk_queue_command_pools_cleanup (#27353)` (robustness fix, not a break)
  - `vulkan: tiled transpose for 0<->2 permuted CONT (#26585)`
  - No HIP-specific entry in what I read; no gfx1151-named break.
- Practical note: v0.2.0 also bundles "nightly build: b10566" as its asset pointer (release body). So the b-line continues at b10566+ after yesterday's b10545.
- **Not affected at runtime by this**; only relevant on rebuild. spirv-headers remains the documented Vulkan build requirement (carried from 08-20, unchanged).

## 2. STATE CHANGE — unsloth PR #5301, first flagged open-status-unknown 2026-08-21, is now **MERGED**
- "fix/strix halo and windows AMD ROCm support" — **Merged** into main by danielhanchen.
  - https://github.com/unslothai/unsloth/pull/5301 (opened page: "Merged, danielhanchen merged 184 commits")
- What it covers per the author's own docs page (opened, search result): "Windows and Strix Halo: native Windows ROCm installation, runtime compatibility patches, unified-memory reporting, and WSL support. (#5301, #6227)" — https://unsloth.ai/docs/basics/amd
- I did **not** read the 184-commit diff; I am claiming merge state + stated scope, not behavior. No claim that it fixes #5301's specific VRAM-exhaustion/ring-buffer symptom unless the commit list says so — treat as "the Strix Halo fix PR from last month is now in main".
- The actionable trap from 08-20 is unchanged: `pip install -U unsloth` can still pull PyPI CUDA torch and silently replace your gfx1151 ROCm build. Re-check `torch.version.hip` after any unsloth upgrade. (Carried; no new evidence either way.)

## 3. STILL OPEN — ROCR busy-spin bug (your torch wheel family), **unchanged**
**Likelihood: HIGH — still the top gfx1151 risk on this machine.**
- TheRock issue **#7051** — "gfx1151: bundled ROCR 1.21 AsyncEventsLoop busy-spins one full core permanently after any GPU op (nightly AND 7.14 production wheels)" — re-opened today: **state=open, label `status: triage`, assignee adityas-amd, last updated 2026-08-13, 4 comments, closed_at=null.**
  - https://api.github.com/repos/ROCm/TheRock/issues/7051 (opened today; full body)
- **No new activity since 2026-08-13** — no fix, no comment, no closure in the last ~9 days. Your installed `torch 2.11.0+rocm7.13.0` remains in the affected wheel family. Watch for the one-core-pegged-at-100%-after-first-GPU-op symptom in any long-lived process.
- The issue's repro explicitly names `whl-multi-arch/` (TheRock production channel) `torch 2.12.0+rocm7.14.0 [device-gfx1151]` as **also** spinning — so a jump to 7.14/TheRock production wheels is **not** a clean escape for this bug; ROCR 1.18 (released-channel 7.2 line) is the one arm that measured clean on the reporter's box. Worth remembering before you plan a wheel-channel switch.

## 4. NO CHANGE — torch / ROCm 7.14 / gfx1151 nightly index
- Latest torch on the gfx1151 nightly is **still ahead of your 7.13.0** (issue #2849 documented `7.11.0a` builds in that channel, and TheRock production moved to 7.14.0 on 08-20's reading) — I did not re-open the nightly index today and make no new version claim.
- No new CUDA-substitution report beyond the unsloth trap in item 2.
- CMake 4.x / hipcc: no new finding surfaced today (not in search results). Carry the rule: keep CMake `< 4` for HIP builds.
- **No new CVEs** found in today's searches against any watch-list item.

## 5. NO CHANGE — transformers v5.15.1 (you're on 5.5.0)
**Likelihood: low unless you actively upgrade.**
- Latest via API re-open today: **v5.15.1, published 2026-08-19** (patch release; body: DFlash/MTP candidate generator fixes + Lanczos→bicubic fallback on CUDA + gemma4 video-to-device fix). No breaking-change claims in the body.
  - https://api.github.com/repos/huggingface/transformers/releases/latest
- The trust_remote_code break class (v4-era `transformers.utils` imports in Hub model code, issues #44561 / #45020) is unchanged in state per 08-20's opening; not re-verified today.
- **Note:** the "lanczos→bicubic fallback on **cuda**" commit in 5.15.1 explicitly reads `cuda` in the patch note — your ROCm path is unaffected by that specific fix, but it also means the original bug path is cuda-named and may not even touch you.

## 6. NO CHANGE — ollama v0.32.15 (you're on 0.32.14)
- Re-open via API today: **latest is still v0.32.15, published 2026-08-19**. No v0.32.16+ exists.
  - https://api.github.com/repos/ollama/ollama/releases/latest
- One small new observation from reading the asset list today (not a break, just noting): the **`ollama-linux-amd64-rocm.tar.zst`** asset is ~1.05 GB and is listed as the default ROCm-flavoured Linux build — meaning the ROCm path is a first-class first-party artifact and you should prefer it over the "generic" `ollama-linux-amd64.tar.zst` if you rebuild.
  - https://github.com/ollama/ollama/releases/tag/v0.32.15 (asset list via API)

## 7. NO CHANGE — OpenWorker
- Re-open via API today: **latest is still v0.1.7 (published 2026-07-30)** — no newer release in between. No CVE or breaking-change note surfaced.
  - https://api.github.com/repos/andrewyng/openworker/releases/latest

## 8. NO CHANGE / UNVERIFIABLE — LibreChat (librechat-dev:latest) and kernel 7.0.0-29-generic
- **LibreChat:** I could not verify a new version today — my two attempts to reach `danny-avila/LibreChat/releases` and `librechat/librechat/releases` via the GitHub API both **returned 404**, likely a repo/reorg rename. The project is still active per search results (`ghcr.io/danny-avila/librechat-dev` is still the live GHCR namespace; docs and site updated mid-2026). **I will not re-report a version or CVE I did not read directly.** If it's important, next run should re-resolve the repo first.
- **kernel 7.0.0-29-generic:** no new gfx1151/amdgpu regression report against this kernel in today's searches. Carry forward 08-20's note: known Strix Halo kernel-side complaints (idle clocks, VRAM underflow) are 6.x-generation reports from late 2025, not your 7.0 series.

---

## Net assessment
Two state changes since yesterday, both in your favor or informational:
1. **unsloth PR #5301 — Strix Halo and Windows AMD ROCm support — is now MERGED.** Last run I recorded it as "status not verified / probably open". First flagged 2026-08-21, now closed-merged.
2. **llama.cpp shipped its first semver STABLE v0.2.0** — dual-versioning begins (semver for stable, `bNUM` for nightly). Vulkan-path changes are in it; no HIP or gfx1151-specific break in the changelog I read, and none affects you until a rebuild.

The one still-open risk (ROCm busy-spin, TheRock #7051) is **unchanged** — no new comments, no fix, last touched 2026-08-13. Everything else on the list is quiet. One operational caveat carried forward: a move to TheRock 7.14 production wheels is **not** a clean escape from #7051 — the reporter's matrix specifically flags `torch 2.12.0+rocm7.14.0` on `whl-multi-arch/` as still spinning — and ROCR 1.18 (released channel 7.2) is the one arm they measured clean.

## Search budget used
4 tavily searches (LibreChat CVE sweep; unsloth #5301 status; LibreChat version; plus one combined context call). API fetches (outside search budget, used as "source of record" for version claims): llama.cpp latest, ollama latest, unsloth latest, transformers latest, TheRock #7051, unsloth PR #5301 HTML, OpenWorker latest, LibreChat API ×2 (404'd). No context7-query-docs calls needed this run — all library items are versioned releases with GitHub release pages, not library-usage docs.
