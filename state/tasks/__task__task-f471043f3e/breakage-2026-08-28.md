# Dependency & breakage watch — 2026-08-28

Watch list: ROCm 7.1.0 · llama.cpp 4df29be4f (Vulkan+HIP) · ollama 0.32.14 · torch 2.11.0+rocm7.13.0 (gfx1151 nightly) · transformers 5.5.0 · unsloth (AMD/gfx1151) · LibreChat librechat-dev:latest · OpenWorker (andrewyng) · kernel 7.0.0-29-generic.

Continuation run — first check since 2026-08-27 (breakage-2026-08-27.md). FOCUS.md (rewritten 2026-08-24) puts workstation-stack, the OpenWorker fleet, and openEvolve Phase 3 in play; tags follow. **One net-new release since 08-27: unsloth shipped v0.1.804-beta.** Everything else is confirmed unchanged via direct GitHub API (ollama v0.33.1, transformers v5.16.1, llama.cpp v0.3.0, OpenWorker v0.2.1). The top gfx1151 runtime risk is unchanged — re-verified via API this run.

**Budget, stated plainly up front:** all six `tavily-tavily_search` calls failed this run with `getaddrinfo EAI_AGAIN api.tavily.com` (the search backend is unreachable, same as 08-26 and 08-27). No net-new searches were run, so no new ROCm/graphics-stack regression could be *discovered*. Status of the standing gfx1151/ROCm/torch items is therefore "**unchanged from 08-27, unexpanded**" — not "confirmed clean." Version-of-record items below were all verified directly via the GitHub REST API (outside the search budget), not from memory.

---

## ON-FOCUS

### 1. STATE CHANGE — unsloth: v0.1.803-beta → **v0.1.804-beta** (2026-08-27 13:09 UTC)
**ON-FOCUS (workstation-stack / the training path — FOCUS item 4). Likelihood: LOW.**

- **Verified via the releases API** this run: `https://api.github.com/repos/unslothai/unsloth/releases/latest` → tag **v0.1.804-beta**, `published_at: 2026-08-27T13:09:58Z`, title **"Qwen3.8-Flash-Next + GLM-5.3-Flash"**. Up from v0.1.803-beta on 2026-08-27's report (published 2026-08-25).
- I read the release metadata (tag, title, timestamp, asset list) via the API, not the full prose body, so the concrete changes are limited to what that metadata states: it is a **model-adapter addition release** (Qwen3.8-Flash-Next, GLM-5.3-Flash) — consistent with the pattern where unsloth's betas track new model adapters. There is **no gfx1151/AMD-specific claim** in the metadata I read, and no note that I can open about the gfx1151 build.
- **Why this is on your watch at all (carry-forward, not a new break):** unsloth is the package whose `pip install -U unsloth` has a documented tendency to pull the wrong torch (CUDA instead of ROCm — the 2026-08-26 verified Strix-Halo trap). So the practical read of "a new unsloth beta shipped" is: if you `pip install -U unsloth` again on this box, **re-affirm the standing instruction from 08-26** — re-install torch from the gfx1151 nightly index and check `torch.version.hip`; if bitsandbytes is pulled in, set `BNB_ROCM_VERSION=71`; and if an MoE-probe import runs, set `UNSLOTH_MOE_BACKEND=native_torch`. The version bump itself is not a known break vector.
- Net: newer version shipped, model-adapter-additive in nature, **no new gfx1151 break** visible. Re-affirm the reinstall-from-gfx1151 guardrail.

### 2. CARRY-FORWARD (UNCHANGED) — TheRocks gfx1151 runtime risks (top gfx1151 risk)
**ON-FOCUS (workstation-stack / the training path). No movement — re-verified via API.**

- **TheRock #7051** — state=**open**, `status: triage`, assignee **adityas-amd**, **4 comments**, created 2026-08-01, last updated **2026-08-13** (verbatim title, opened in full this run: *"[Bug] gfx1151: bundled ROCR 1.21 AsyncEventsLoop busy-spins one full core permanently after any GPU op (nightly AND 7.14 production wheels) — release-channel ROCR 1.18 clean on same kernel"*). First flagged 2026-08-18, **zero movement** since 08-26. Still the top gfx1151 runtime risk.
  - https://api.github.com/repos/ROCm/TheRock/issues/7051 (opened this run)
- **ROCm/legacy-rocm-build #6634** (the gfx1201 second report, flagged 2026-08-24) — unchanged: state=open, 0 comments, created 2026-08-20. Same environment family as your kernel. (Did not re-open; carry 08-26 verbatim.)
- **TheRock #5284** carries its good-news note: your exact wheel `torch==2.11.0+rocm7.13.0` is the *fixed* one for the 7.12 `_grouped_mm` null-HIP-kernel crash. (Snippet-only on 08-26; not re-opened — no reason to, it carries unchanged.)

### 3. CARRY-FORWARD (UNCHANGED) — transformers 5.5.0 sits on/above the 5.x line (latest v5.16.1)
**ON-FOCUS (workstation-stack). Low risk unless you upgrade.**

- Latest re-confirmed via API this run: **v5.16.1**, published 2026-08-26 14:50 UTC — same as 08-27. No newer release. (Body carries the GLM-5.3-Flash model addition + the ESMFold2 path fix #48186 and TP-restore BC #48300 from 08-27; those are additive, not breakers, and your 5.5.0 predates them.) Your 5.5.0 is unaffected. **No action.**
  - https://api.github.com/repos/huggingface/transformers/releases/latest (opened this run)

### 4. NO CHANGE — ROCR `AsyncEventsLoop` / gfx1151 kernel-family and the trust_remote_code class
**ON-FOCUS. Unchanged.** No net-new CVE with a directly-opened primary source; the snippet-level trust_remote_code / RCE framing from 08-24–08-26 carries forward unverified, nothing to add.

---

## ADJACENT (capped at three)

### A. NO CHANGE — ollama, llama.cpp, OpenWorker (version-of-record)
- **ollama v0.33.1** (2026-08-26 18:09 UTC) — unchanged; the ROCm Linux wheel `ollama-linux-amd64-rocm.tar.zst` is still present in it; the `cmake: make external compat patches idempotent` hardening commit from 0.33.1 carries forward as an unverified-on-CMake-4.x build-time note. (Latest API opened this run.)
- **llama.cpp v0.3.0** (2026-08-25 10:22, commit c1d0e7a) — unchanged; nightly still **b10621** from 08-27's read. `ci: Restore ROCm job for Ubuntu (#27399)` and the spirv-headers Vulkan requirement carry forward unchanged. (Latest API opened this run.)
- **OpenWorker v0.2.1** (2026-08-25 17:07) — unchanged; the v0.2.0 "Updating is recommended" security/approval-handling line carries forward. (Latest API opened this run.)

### B. NO CHANGE — ROCm 7.1.0 / torch 2.11.0+rocm7.13.0 / kernel 7.0.0-29-generic
- **No net-new** ROCm release, CUDA-substitution report, or kernel regression could be *discovered* — search tools were down, so nothing was expanded. **Carry forward from 08-27:** the unsloth-installed-torch CUDA-substitution trap and TheRock #5284 "fixed wheel" framing remain the standing torch/ROCm concerns; the kyuz0 Strix-Halo firmware note ("do NOT use linux-firmware-20251125; kernels older than 6.18.4 unstable on gfx1151") carries from 08-26, still snippet-only and unverified.

### C. NO CHANGE — LibreChat
- **LibreChat librechat-dev:latest** — unverifiable this run (search down); no new release/CVE surfaced, no expansion. Stale, no finding to report, as on 08-26/27. (Repo was confirmed live 08-26; not re-verified here — no reason to, absent a new lead.)

---

## Net assessment
The **only net-new thing since 08-27 is unsloth v0.1.804-beta** (2026-08-27 13:09 UTC), an adapter-additive beta ("Qwen3.8-Flash-Next + GLM-5.3-Flash") with **no visible gfx1151/AMD-specific break** — but because unsloth is the package historically responsible for the wrong-torch CUDA-substitution trap on this box, the practical action is the standing one: any fresh `pip install -U unsloth` here re-triggers the need to re-install torch from the gfx1151 nightly index and set `BNB_ROCM_VERSION=71` / `UNSLOTH_MOE_BACKEND=native_torch` as before. The top gfx1151 runtime risk (TheRock #7051, re-opened in full this run — open, triage, 4 comments, last touched 2026-08-13, zero movement since 08-18) and legacy-rocm-build #6634 carry forward unchanged; ollama v0.33.1, transformers v5.16.1, llama.cpp v0.3.0, and OpenWorker v0.2.1 are all confirmed unchanged via direct API.

**Budget limitation, stated plainly:** all six `tavily-tavily_search` attempts returned `getaddrinfo EAI_AGAIN api.tavily.com` — the search backend is unreachable this run (3rd consecutive run so: 08-26, 08-27, 08-28). **0 of 6 searches used**, and 0 net-new searches were possible. The status of the standing ROCm/gfx1151/torch-wheel/CMake-4.x/spirv-headers/trust_remote_code items is therefore "**unchanged from 08-27, unexpanded**" — I did not attempt to reconstruct plausible release notes. Every version-of-record value and every state/label/comment count in this report was verified directly via the GitHub REST API and by opening the release-issue pages, not from memory.

## Budget & sources
**0 of 6 tavily searches used** — Tavily API returned `getaddrinfo EAI_AGAIN` on every attempt. No results reconstructed from memory.

**Direct API opens (version-of-record / state, outside search budget):**
- `https://api.github.com/repos/unslothai/unsloth/releases/latest` → **v0.1.804-beta** (published 2026-08-27T13:09:58Z, "Qwen3.8-Flash-Next + GLM-5.3-Flash") — net-new release
- `https://api.github.com/repos/unslothai/unsloth/releases/377832231` → release metadata (tag, title, assets)
- `https://api.github.com/repos/ROCm/TheRock/issues/7051` → state=open, triage, adityas-amd, 4 comments, last updated 2026-08-13, verbatim title (re-opened; unchanged)
- `https://api.github.com/repos/ollama/ollama/releases/latest` → **v0.33.1** (published 2026-08-26 18:09)
- `https://api.github.com/repos/huggingface/transformers/releases/latest` → **v5.16.1** (published 2026-08-26 14:50)
- `https://api.github.com/repos/ggml-org/llama.cpp/releases/latest` → **v0.3.0** (published 2026-08-25 10:22)
- `https://api.github.com/repos/andrewyng/openworker/releases/latest` → **v0.2.1** (published 2026-08-25 17:07)

**Snippet-only / unverified, carried from 08-26/27 (no new verification — search tools down):** TheRock #6634, #5284; unsloth Strix-Halo workaround article; trust_remote_code / RCE bulletin; kyuz0 firmware/kernel note; strobes.co / nhimg.org CVEs.
