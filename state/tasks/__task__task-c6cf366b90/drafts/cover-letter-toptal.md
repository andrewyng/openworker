# Cover Letter — Toptal: AI/ML Engineer for an AI-Driven E-Commerce Platform

> Posting: posted ~1 day ago, apply before **Sep 16, 2026**, remote (anywhere).
> Role core: autonomous e-commerce AI agents, conversational commerce,
> one-prompt checkout, fine-tuning open models (Llama/Mistral), GPU infra,
> full-stack (MongoDB/Express/React/Node), REST APIs in production.

---

Hi,

I'm an AI/ML engineer who has built exactly the kind of agent-led, LLM-integrated
systems this platform needs — and I've owned the full path from model to
production API. Here's how my work lines up with the three initiative areas in
the posting.

**Agent-led product experiences.** At Mutual of Omaha I built an ETL pipeline in
which small language models act as *autonomous agents*, deduplicating medical and
insurance claim records end-to-end, and a RAG system for semantic querying over a
large document corpus that cut manual review time. That's the same "agent acts on
a goal, I own the loop and the guardrails" pattern your internal AI-employee and
one-prompt-checkout agents need for a high-stakes domain where a wrong action has
cost. I'm comfortable owning that loop, not just calling a model.

**Model integration + open-model fine-tuning.** I work in the LLM stack
day-to-day: HuggingFace Transformers, LangChain, DSPy, Unsloth, Ollama. I've
designed inference APIs and owned the full deployment lifecycle — provisioning,
containerization (Docker), monitoring, and retraining pipelines — on Google Cloud
Vertex AI, where I architected and deployed a personalized content/product
recommendation system at scale. The recommendation work is directly transferable to
"dynamic site customization" and product-suggestion flows in an e-commerce surface.

**Production REST APIs and full-stack.** I've designed and exposed RESTful
inference APIs consumed across multiple application surfaces. I write TypeScript
and use Next.js, and I integrate third-party APIs into production apps daily — the
posting's Stripe/Twilio/OpenAI integrations map to API-integration work I already
do.

Where I want to be candid about the gap: the posting asks for fine-tuning open
models like Llama or Mistral and hands-on GPU infrastructure setup. I have the
fine-tuning tooling (Unsloth, Ollama) and the full MLOps side, but my resume shows
fine-tuning and GPU-infra as *capabilities* more than a single headline project.
If this platform is standardizing on Llama/Mistral, I can walk through exactly how
I'd set up the fine-tune → eval → deploy pipeline — and I'd be glad to do a
hands-on task against your stack before we commit.

I work fully remote and US-based (FL). I'm based in a range of time zones and can
overlap comfortably with North American hours.

Thanks for considering.

Jonathan Beale
jonaston015@gmail.com · github.com/iconbaypark2900
