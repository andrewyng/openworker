# Weekly review — 2026-W36 (week of Monday 2026-08-31)

Continues the weekly record. This is the first weekly report; no prior
`reports/weekly/` file exists (that directory did not exist this run), so there
is nothing to carry forward beyond FOCUS.md's own carried items and the
knowledge threads. FOCUS.md was rewritten 2026-08-31 by the "derive" automation.

Every line below traces to a file read this run. Primary-source git logs were
read directly on-box; the daily `ledger`/`repo-activity` outputs were read
directly; the knowledge threads were read directly. The agpack test-suite
numbers come from the `agpack` thread (read this run), which is the
session-level record of the 08-30 build.

ON-FOCUS = bears on a project or open question named in FOCUS.md. ADJACENT does
not, but is worth knowing.

## Moved

**agpack — the "build" (ON-FOCUS).** The week's headline move: the 5-step
verifiable-agent harness shipped its final build step. Commit `60aaaf1`
(2026-08-30, primary git source, `/home/iconbaypark2900/dataScience/agpack`)
lands `agpack/tools/metered.py` — Step 5 metered / pay-per-tool-call access
(MeteredTool, Fuel, CostPolicy, MeterLedger, MeteredBroker, InsufficientFuel)
plus `tests/test_metered_access.py`. This is the only second commit on top of the
08-19 `50c2d2b` import from openworker-workspace. Per the `agpack` thread (read
this run) the suite grew 203 → 231 (Step 4 portability proof, 08-31) → 276
(Step 5, 08-30); the harness is described as *fully built* (Steps 1–5), with the
Step 4 proof itself carrying an honest caveat: it is not a *true* cross-engine
test because this host has no wasmtime/wasmer. State: **build complete; the
deployment/ship decision that FOCUS calls P0 is still open** — no deployment
commit exists. Evidence: `git log` (agpack, read this run); `knowledge/threads/agpack.md` (read this run).

**OpenWorker — the agent app itself (ON-FOCUS).** 40 commits in the 14-day
window, latest `281990f` (2026-08-30, git source, `/home/iconbaypark2900/openworker`):
"state: carry four days, including the mornings that failed" — a direct response
to the carried unattended-run question. Same-day commits: `641992f` "desktop:
one place that knows where the user's screen is"; `f2f77e3` "automations:
catch-up waits for the model, and goes one at a time"; `e51280a` "picker: a
folder dialog that cannot open says so" (an honest stop-marker). State: the
carried question survives but the "carry the failed mornings" commit is concrete
engagement with it, not just restating it. Evidence: `git log` (openworker, read
this run).

**dcode-stack — the serving brain, decode_proxy (ON-FOCUS).** Moved from
"Went quiet" → Active this run. 51 commits in the 14-day window, all 08-26/27, all
iconbaypark2900. Primary `git log` tail this run shows the arc: `f19c6a8`
(08-26) vLLM on :5100 + on-demand llama.cpp → `e24cce7` auto-classifier
(granite failed 5/6 destructive batches) → `94ade62` "Size the serving profile
for four consumers" → `b7fa880` (08-27) "a dead CUDA engine exits 0, so
on-failure never restarted the brain" → `9bc3b30` (08-27) "report the engine
that is serving." The open question in FOCUS ("why does a dead CUDA engine exit
0 so on-failure never restarts it?") is **answered by a commit** — the exit-0
behaviour was the bug, and it is now in the record. State: came back from
08-16 quiet and then fixed the "dead engine exits 0" problem it was posed about.
Evidence: `git log` (dcode-stack, read this run).

**workstation-stack — the deployed stack (ON-FOCUS).** 10 commits in window,
back from 08-21 quiet. `639fd2d` (2026-08-29, git source,
`/home/iconbaypark2900/workstation-stack`): "Seven LibreChat secrets existed on
one disk and in no backup"; `468eae4` (08-28): "The mirror never protected the
secrets." State: the secrets-protection model is under repair — a real fix
attempt this week, not just a flag. Evidence: `git log` (workstation-stack, read
this run).

**Job search & funding pipeline (ON-FOCUS).** No move on the thing that matters
(is anything *sent*?): 10 daily `jobs-*.md` (08-16→08-31) all drafts. Two
genuine state changes, however — Horizon 2027 Fellowship **CLOSED**
(deadline 2026-08-30 11:59pm AoE has passed; first flagged 08-26/27 as "OPEN,
~3 days out", drafts were carried 08-26/27/28 but never submitted, so the 2027
window is lost — ledger-2026-08-31, read this run) and two fresh no-PhD targets
went live: MATS Residency Winter 2027 and the re-opened Corrigibility Research
Fund (ledger-2026-08-31). Evidence: `knowledge/ingest/ledger-2026-08-31.md`
(read this run).

## Still open

- **agpack P0 — unblock deployment and ship.** Confirmed git remote is
  `git.home.arpa:2222/iconbaypark2900/agpack.git` (read this run), but there is no
  deploy/ship commit, only the build. *Would close it:* decide "ship" means
  publish to the home repo + a first external-facing consumer (Direction A), then
  do that one commit.
- **agpack Conformance Tier B (live-spec fetch + draft-drift) — how legible must
  it get?** Named in FOCUS from the 08-30 sessions; nothing landed to answer it.
  *Would close it:* name the first outside reader and wire the live-spec fetch.
- **How vLLM + llama.cpp share one proxy, routed by model name.** The routing
  mechanism is now in `decode_proxy` code (08-26/27) — the *why does a dead engine
  exit 0* half is answered. *Would close it:* confirm the on-demand llama.cpp path
  still works end-to-end (last activity 08-27, four days ago — not re-verified this run).
- **Local-27B fleet survives unattended with no silent failure.** Carried from
  08-23; the "carry the failed mornings" commit engaged it but did not prove it.
  *Would close it:* one full unattended week with zero silent-failure fall-backs.
- **Is the job/grant pipeline converting to *sent* applications, or only to
  drafts?** Unchanged 08-24 → 08-31. *Would close it:* one application actually
  sent. (See Unexamined.)

## Unexamined

*Method: grepped the accumulated `.md` outputs this run (not from memory). An
item qualifies only if it has appeared three-plus times and still shows no state
change.* All four sections are non-empty.

1. **GitHub MCP gateway is unreachable — repo-activity can see 0 of 10 repos.**
   Appeared **4 consecutive runs**: 08-27, 08-28, 08-31 identical
   (`MCP error -32603: fetch failed` on all 10 repos, `repo-activity-2026-08-31.md`
   read this run), preceded by `Authentication Failed: Bad credentials` on
   08-24 (different failure, reachable gateway). The `github-github-list_commits`
   gateway thread (read this run) confirms the same 4-run span. **First appearance
   of the outage: 08-27** (the 08-24 credentials error is the earlier, distinct
   root). Consequence: every project's "did it move last 24h" is reported as
   *unknown, not no-activity* — i.e. 10 tracked repos are unobservable by the
   automation. **Smallest action that ends it:** refresh/replace the gateway token
   or fix the gateway — a single infra fix, and the brief returns to 10/10. This is
   the clearest "keep being told and keep ignoring" item on the board.

2. **The job/grant pipeline produces drafts, never a sent application.**
   **10 daily `jobs-*.md`** (08-16→08-31) all report only drafts; "No
   application" recurs across them (this run's 08-31 jobs file repeats it), ~106
   "draft" mentions across the set. First appearance: the 08-16 window (jobs briefs
   start 08-16). The cost is now concrete: **Horizon 2027 closed** with those
   drafts never submitted — the 2027 window is gone. **Smallest action that ends
   it:** submit one draft this week (the fresh MATS Residency is the best live
   fit). No state change since 08-16.

3. **TheRock #7051 — ROCm gfx1151 segfault / AsyncEventsLoop 100%-CPU spin.**
   **8+ breakage briefs** (08-18, 20, 21, 22, 24, 26, 27, 28, 31 — `breakage-*.md`,
   read this run) carry it `state=open`, **unchanged since 08-18**. The related
   issue #5284 already carries a good-news note (a fixed wheel), but #7051 itself
   has not moved. First appearance: 08-18. **Smallest action that ends it:** act on
   the #5284 resolution and comment/triumphantly close #7051, or let it sit (it is
   a tracked upstream issue, so inaction is defensible — flagging it for the count).
   (ADJACENT: this is a dependency watch, not one of the 5 active projects.)

## Decisions waiting on me

1. **agpack — ship it or leave it built?** Yes → make the first deploy/external
   "ship" commit to the home repo (Direction A, external-facing) and call P0 done.
   No → it stays a local, fully-built 5-step harness; "ship it" remains an open
   decision by choice.

2. **The GitHub gateway — fix it this week?** Yes → one token/infra fix restores
   10/10 coverage across every tracked repo for the automation. No → accept a
   permanent blind spot where all 10 projects' last-24h activity is reported as
   "unknown."

3. **The job pipeline — send one application this week?** Yes → the pipeline
   produces a sent application and stops the "drafts only" pattern (with Horizon
   2027 as the standing warning of what that costs). No → the 37+ drafts and the
   already-lost 2027 window stay pure waste.

---

## Adjacent (cap 3)

- **arXiv → Qdrant corpus pipeline is healthy and moving** (ADJACENT, ON the KB
  not the 5 projects): the ingestion job grew its own collection **21 → 31**
  today, storing 10 papers via HTTP; dedup correctly suppressed one cross-file dup
  and a corpus-unique security survey was stored. Evidence:
  `knowledge/ingest/ledger-2026-08-31.md` (read this run).
- **US restricts Egypt's Banque Misr UAE ops over Iran facilitation** (ADJACENT,
  news, 2026-08-31) — threads into the carried Iran line; low decision value.
- **India shipping ~1M barrels of gasoline to Russia** (ADJACENT, 2026-08-31) —
  fuel-macro, recorded, not stored.
