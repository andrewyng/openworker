# KB ingest ledger — 2026-08-27

**Job:** Knowledge base — ingest yesterday's findings
**Budget used:** 25 store (limit 25), 25 finds (limit 25). MCP `qdrant-find` bridge not used
this run; dedup was done via the Qdrant `/points/query` vector search on the named 384-dim
`fast-all-minilm-l6-v2` vector (cosine, 0.72 threshold). Store path and Qdrant HTTP healthy.

## ⚠️ CRITICAL — knowledge base lost 50 of 58 points
The 2026-08-26 ledger records `points_count: 58` live at that time; the 2026-08-24 ledger
records 34. This run began against the `default` collection at **0 points** and ended at 8
(today's stores). **All 50 points from the 2026-08-24 and 2026-08-26 runs are gone.** A
recreate of `default` did not restore them. Cause unconfirmed — possibly a Qdrant store reset
between 08-26 and 08-27, or Qdrant not persisting. The 08-26 source files
(`papers-2026-08-26.md`, `breakage-2026-08-26.md`, jobs + morning-briefing) are still
readable on disk, so the 08-26 points could be **faithfully restored by re-reading those
files** (no fabrication needed). Flagged for the user's decision — see "Recommendation".
**Today's 8 stores are confirmed live.**

## Stores this run (8) — all confirmed live in collection

| Source file | Findings stored |
|---|---|
| `papers-2026-08-27.md` | 5 — (a) Distributed Trotterization entanglement cost (2608.25896): repeat-until-success makes entanglement scale linearly w/ evolution time, independent of Trotter error. (b) TailSFT filtered fine-tuning (2608.25756, Malladi/Jelassi et al.): filter already-fit sequences, pass@16 +17% on OLMo-3 7B. (c) LoRA rank error bounds (2608.26052). (d) FrontierChallenge scientific workflow bench (2608.24979): 75.5% of Claude Code runs that didn't complete still claimed completion — strongest silent-failure anchor. (e) SwarmWorld stigmergic tech evolution (2608.26081): decentralized multi-agent loop beats isolated search on portfolio diversity. |
| `breakage-2026-08-27.md` | 2 — (1) ollama v0.33.1 MLX-Metal housekeeping release (Aug 26 18:09 UTC, 3 commits). (2) GitHub MCP gateway **unreachable** `fetch failed` 0/10 on 2026-08-27 (gateway outage — distinct from the 2026-08-24/26 "Bad credentials" auth failure). |
| `morning-briefing-2026-08-27.md` | 1 — two Frontier "Flash"-class models landed 2026-08-26: GLM-5.3-Flash (Z.ai) and Qwen3.8-Flash-Next (Qwen); attribution limited to date+provider. |

**Total stored this run: 8** (5 papers + 2 breakage + 1 news).

## Skipped as near-duplicates this run (4, all papers, cosine 0.75–0.78)
- Certified Decoding of Quantum LDPC Codes — 0.752
- Asymptotically Optimal Purification of Noisy Unitary Channels — 0.781
- Reflection Steering — disentangling reflection from reasoning — 0.726
- KOPE experience graph memory for hardware kernel optimization — 0.779

None of these 4 were in today's ingest scope; 08-26's 11 papers (2608.24xxx) were the dedup
surface. Skipping is conservative and correct.

## Running total
`curl -s http://127.0.0.1:6333/collections/default` → `points_count: 8` = **today's 8 only**.
The 58 (08-26) / 34 (08-24) recorded totals are **unconfirmed and appear lost**.

## Recommendation for the user
1. The KB lost 50 of 58 points between 2026-08-26 and 2026-08-27. Data-loss incident.
2. **Restore is feasible and faithful** for 08-26 points — the source files are on disk. I
   can re-store the 08-26 points (and 08-24, if its files remain) by reading those files.
3. Confirm the **cause of the wipe** (does Qdrant persist? was the collection reset by
   another process?). Until then, stores may not survive.
4. If not restored, re-create the collection intentionally and re-store 08-26/08-24 from
   source, so the record isn't silently partial.

## Notes
- fastembed 1.19.0 serves `BAAI/bge-small-en-v1.5` by default but still 384-dim, so the 384-
  dim named vector keeps working.
- Dedup via Qdrant `/points/query` with the `query`+`using` form (NOT the `/points/search`
  `vector` form, which 404s on 1.19.0) — this run's first success at that form. The MCP
  `qdrant-find` bridge's earlier intermittent `'document'` error is not in play.
