---
id: agpack-sandbox
title: agpack-sandbox
state: active
updated: '2026-08-28'
tags: []
---
**Now:** Step 2 (sandbox) fully implemented and green: capabilities.py + imports.py (host-import surface) + limits.py (fuel/memory/wall-time meters with a 0..8 scope fuel table) + host.py (load→validate→instantiate→dispatch→meter→record engine that gates dispatch through limits.check()). Full sandbox suite: 108 tests green — imports 28, host 25, limits 30, capabilities 25. README Status rewritten (steps 1-2 implemented, 3-5 stub). tests/test_artifact_bundle.py is NOT part of the suite (imports pydantic, not a dependency).

## History
- 2026-08-28 — Step 2 grew past capabilities.py: host.py's load→validate→instantiate→dispatch→meter→record engine now GATES every dispatch through limits.check() (the meter gate). It captures the raised LimitError instead of propagating it, records the outcome on RunResult.limit, and writes a "dispatch" audit record with fields `rejected`, `limit_violation_code`, and `fuel_meter_bug_suspect` (a wall-time kill with fuel still within budget). Fuel is metered by SUMMING import_call.fuel_delta (net.fetch=40, fs.write=20, fs.read=15, memory.set=5, memory.get=4, random=3, emit.text=2, clock.now=1). (source: /home/iconbaypark2900/dataScience/agpack)
- 2026-08-27 — Full Step 2 (sandbox/capabilities.py) is implemented: Scope enum (closed 8-set), CapabilityParams (bounded origins/fields, max_bytes), CapabilityPolicy (frozenset+params), CapabilityViolation, and platform_max_cap(). The model replaces the earlier stub (was NotImplementedError). Policy names carry the agpack/ prefix and require agpack/<area>.<verb> grammar. Validation: unknown/wildcard/grant.* rejected; duplicate rejected; unbounded lists (>16) rejected via MAX_PARAM_LIST_ITEMS=16; per-scope params typed (net.fetch↔origins only). tests/test_sandbox_capabilities.py: 25 tests green. No crypto/WASM. Did NOT touch host.py/limits.py/imports.py (out of scope, they depend on this).
- 2026-08-27 — agpack stub modules are import-stubs that raise NotImplementedError on import: sandbox/imports.py, sandbox/host.py, trust/audit.py (AuditRecord/AuditLedger/replay), portability/driver.py (RuntimeDriver Protocol), portability/profiles.py, portability/compat_harness.py, tools/metered.py. Only sandbox/capabilities.py + sandbox/limits.py + artifact/packager+schema+validator import cleanly. Consequence: imports.py + host.py (Step 2) MUST be written against the *protocols* described in the docstrings (duck-typed), importing trust/portability only under TYPE_CHECKING, so they test without real WASM/driver/network/crypto/pydantic. Budget fuel is reconstructed by SUMMING import_call.fuel_delta (per driver.py: "the fuel meter is a driver-internal concern; the host reads the fuel delta out of the ledger"). audit ledger contract used by host: .append(record). Runtime deps not installed yet: pydantic (artifact), wasmtime/wasmer (drivers). pyproject: mypy strict=true, ruff line-length 100. Run tests scoped: .venv/bin/python -m pytest tests/test_sandbox_*.py -q (artifact tests fail to import pydantic).", (source: src/agpack)
