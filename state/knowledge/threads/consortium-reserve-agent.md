---
id: consortium-reserve-agent
title: consortium-reserve-agent
state: active
updated: '2026-08-31'
tags: []
---
**Now:** Phase 1 (P3) implemented: drift.py + verify-drift verb + committed tests.

## History
- 2026-08-31 — Consortium reserve agent project mapped to 7 phases (design in /home/iconbaypark2900/openworker-tasks/8c006cfd-864/consortium-agent/). 3 pillars from read-only repos: sentinel-local (local multi-agent + PQC ranking) = quantum-safe reasoning, agpack (signed bundle/WASM audit ledger/scoped delegation) = payout-integrity, metered-web-broker (PaymentRail×5 + BudgetEngine + finality flag) = settlement spine. Pipeline stages: observe→attribute→settle-router→payout→disclosure→compliance. Phase 0 = deterministic synthetic corpus (critical path). Gate rule: no phase moves value until previous phase's ledger is reconcileable; Phase 3 payout is risk apex, prefer testnet. Highest-value/lowest-risk = Phase 1 (attribution+disclosure). PQC migration = Phase 6, signer swap at agpack trust boundary.
- 2026-08-31 — Verified STUB vs REAL across the 3 repos. Stubs (must-be-written in consortium package): agpack/cli.py raises NotImplementedError (the 4-verb CLI does not exist — consortium must build its own thin runtime threading audit+signing+delegation+sandbox); sentinel crypto/cbom.py to_cbom+write are TODO; sentinel agents/crypto_posture.py class CryptoPostureAgent is NotImplementedError (deterministic scoring only, LLM for narrative). Delegation Scope enum + mem/fs/net resource namespace are CLOSED and cannot express a payout spend — Phase 3 must add a payout scope/namespace before verify() will accept a payout token (verify is hard-fail). REAL and importable: agpack trust (audit.py LedgerCorrupt on bad records, 4 record kinds dispatch/import_call/delegate/budget), sentinel orchestrator/structured-output/eval pattern, metered core/budget/rails/identity (preflight throws on CURRENCY_MISMATCH, reconcile().ok is the finality gate, draft-caveat on payload field names). REUSE.md + PHASES.md in consortium-agent/ record all of this so no phase re-derives it. (source: /home/iconbaypark2900/openworker-tasks/8c006cfd-864/consortium-agent/)
- 2026-08-31 — Phase 0 (critical path) record contract VERIFIED against agpack/trust/audit.py. The consortium package must build its own runtime (CLI is a stub). The load-bearing contract: AuditRecord fields ordinal/run_id/kind/ts_unix/subject/detail, where kind is one of 4 CLOSED values. dispatch detail = {tool_cid, args_sha256_prefix16, output_sha256, budget_spent}. import_call detail = {scope, resource, arg_fingerprint, host_return, fuel_delta}. delegate detail = {token_id, parent_token_id(None allowed), hop_depth, scope, resource}. budget detail = {budget}. Detail that mismatches kind → LedgerCorrupt (hard fail, names the key). ts_unix is a logical 1-sec clock (not wall time). Arg buffer redacted to 16-hex-char SHA256 prefix only — ledger is a replay ORACLE, not a replay-input. Sentinel config/agents.yaml consensus.policy = {verdict: exact, severity_tolerance: 1, technique_jaccard_min: 0.6, confidence_floor: 0.5, escalate_on_schema_failure: true}. sentinel/eval/metrics.py headline metric = false_negative_rate_on_true_positives (lead with FN rate, not accuracy). (source: /home/iconbaypark2900/openworker-tasks/8c006cfd-864/consortium-agent/PHASE0.md)
- 2026-08-31 — Product name LOCKED: **ConcordCircuit** (fuses "concord" = agreement/consortium + "circuit" = the settlement loop that reconciles). Replaces the placeholder "consortium-agent/consortium" naming in all docs and memory. (source: /home/iconbaypark2900/openworker-tasks/8c006cfd-864/consortium-agent/)
- 2026-08-31 — P1 (Persona/Composition) of the 7-phase ConcordCircuit build is VERIFIED complete as of 2026-08-31: `concord/` composes agpack + sentinel-local + metered, 33-symbol reality table (27 REAL/6 STUB/0 MISSING, egress-zero, byte-deterministic), the one invariant + finality gate in `concord/invariant.py`. Wrote the P2 (Phase 0 = JSONL audit-runtime) prompt to /home/iconbaypark2900/openworker-tasks/a7090ed4-579/P2-prompt.md — it fixes the 3 P2 bugs I spotted while verifying and pre-wires the real `AuditRecord`/`LedgerCorrupt` 4-record contract and the Fraction-based attribution so Phase 0 is pure-math, egress-zero, deterministic. (source: /home/iconbaypark2900/openworker-tasks/a7090ed4-579/P2-prompt.md)
- 2026-08-31 — Phase 1 (P3) of ConcordCircuit is now implemented in repo. New files: `concord/drift.py` (build_drift_ledger base|p117|aave, reconcile_drift, classify_drift, check_drift), `conftest.py` (injects agpack root on sys.path), `test_concord_drift.py` (7 tests), `concord/phase1_NOTES.md`, and `concord/__main__.py` gained `verify drift`. Ave 37.5bps slippage = unabsorbable (loss = 3,750,000 × sum(aave shares=112) = 420,000,000 wei; ok==False). p117 on curve chain = +1-wei residual absorbed by residual scheme (in==out, ok==True). The distinguisher: p117 ok==True / aave ok==False, proving the gate isn't just "any nonzero delta". Base build_corpus() is mutated only by copy — P2's verify invariant control untouched. AVOID re-deriving: P2 prompt was written to /home/iconbaypark2900/openworker-tasks/a7090ed4-579/P2-prompt.md, P3 prompt to .../P3-prompt.md (ConcordCircuit is read-only to this machine). (source: /home/iconbaypark2900/jabCreative/dataScience/concordCircuit/concord/drift.py)
