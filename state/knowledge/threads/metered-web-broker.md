---
id: metered-web-broker
title: metered-web-broker
state: active
updated: '2026-08-30'
tags: []
---
**Now:** 2026-08-30: Tier B model = pinned-required + pin-reference drift; editions.ts field sets verified older⊆pinned⊆live (rename-aware). Building protocol validators, fetch/provenance, and test suite.

## History
- 2026-08-30 — Tier B (2026-08-30) records the live-spec field-name references researched for the metered-web-broker conformance upgrade (fetch live / pin draft / drift-detect per protocol): (1) A2A AgentCard — a2a-protocol.org, latest 0.3.0, prev 0.2.6. Key fields: name, version, description, url, provider{name,url}, capabilities{agentExtensions[], interfaces[], agentInterfaces[], map, messageTypes, pushNotificationConfig, promptNotificationConfig, preferredTransport, additionalInterfaces} (interfaces/preferredTransport/additionalInterfaces are 0.3.0 additions over 0.2.6), skills[], defaultInputs, authentication, securitySchemes, security, transportPreferences{enum http+json|json-rpc|grpc}. Source docs: a2a-protocol.org/v0.3.0/specification and github.com/a2aproject/A2A. (2) AP2 Agent Payments Protocol — ap2-protocol.org/specification. Mandate fields (SD-JWT VPD): user{auth}, merchant{name,url,authentication} (earlier drafts used agent→merchant rename), shoppingIntent, chargeable[], prompt, ttl, priceAmount/priceCurrency, riskPayload{requestId,components[],requested}. (3) UCP — ucp.dev, edition 2026-04-08. Discovery profile at /.well-known/ucp: {ucp:{version, services<>{version,spec,schema,transport,endpoint}, capabilities<>{version,spec,schema,extends}}, keys:[JWK], meta, supported_versions}. Capability/service naming: {reverse-domain}.{service}.{capability}. (4) Web Bot Auth — webbotauth.org (RFC 9421 sig-params). Directory = keys[] {id,type,alg,use,kid,crv,x5c[]} + signatures[]{id,date,alg,target,params['http.request.method'/'http.target.name'],signature,signature_base}, identified via Signature-Agent header; the key directory can double as a UCP key source. These are recorded inside the conformance editions manifests in packages/conformance/src/protocols/editions.ts.
- 2026-08-30 — P1 Tier B progress (2026-08-30): Files created in packages/conformance/src/protocols/ — editions.ts (PROTOCOLS table with older/pinned/live field sets per protocol) and drift.ts (pure drift detection). types.ts now has SpecProvenance/ValidatorContext/ValidationResult.spec/detail + async ValidatorFn; run.ts is async (awaits validators, renders [pinned/live/drift] provenance blocks). cp CLI not yet async. Still TODO: CLI wiring with --protocol/--live/--older pin, protocol validators, fixtures, tests, CI, and verifying live fetch (network not yet confirmed). A2A 0.3.0/0.4.0-dev and ucp.dev URLs confirmed reachable (UCP 200); a2a-protocol.org v0.3.0/0.4.0-dev agentcard paths and all ap2-protocol.org paths return 404 — need real URLs before wiring fetch. (source: /home/iconbaypark2900/dataScience/metered-web-broker/packages/conformance/src/protocols/editions.ts)
- 2026-08-30 — 2026-08-30: Tier B protocol-engine scaffold verified. run.ts/types.ts/registry.ts already Tier B-ready (async validators, SpecProvenance{pinnedEdition,pinnedUrl,liveUrl,liveEdition,drift}, ValidatorContext{protocol?}, detail line). validators.ts: 4 contract validators on ContractCard — separate from protocol validators. conformance package has NO tests + NO test script currently; root vitest globs packages/*/src/**/*.test.ts (must place tests in src). Network fetch works: a2a-protocol.org/v0.3.0/specification/ and ap2-protocol.org/ap2/specification/ both 200. Model: computeDiff(artifact,manifest,pin) — pinned fields always required; pin reference edition 'pinned'/'older'/'live'. pin='pinned' strict; pin='older' => pinned artifact drifts on pinned-only fields (the drift proof); pin='live' => no drift. Renames: AP2 mandate agent->merchant mapped to pinned spelling. Invariant checker lives in check_invariants.mts at repo root. Field sets in editions.ts: a2a{agentCard,capabilities,security}, ap2{mandate,riskPayload}, ucp{ucp,service,capability}, web-bot-auth{directory,key,signature}. (source: /home/iconbaypark2900/dataScience/metered-web-broker/packages/conformance/src/protocols/editions.ts)
- 2026-08-28 — Wire contract fix (2026-08-28): `SelfHosted402Rail.authorize()` was encoding the `x-payment` header as a bare mandate JSON (`b64url(JSON.stringify(mandate))`), but `decodeXPayment`/`encodeXPayment` require the envelope `{ type: 'urn:mwb:self402:payment-mandate:v1', mandate }`. Changed authorize() to call `encodeXPayment(mandate)`. This was the real bug blocking the metered-origin round-trip test (decode threw 'unknown x-payment shape'). The `payload` field of RailAuthorization is still the bare mandate; only the settlement header must be the envelope form. (source: packages/rails/src/selfsettle.ts)
