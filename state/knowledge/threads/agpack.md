---
id: agpack
title: agpack
state: active
updated: '2026-09-02'
tags: []
---
**Now:**

## History
- 2026-09-02 — 2026-09-02 — "Outside view" (an autonomous adversarial-critique automation) ran an explicit argument against the agpack verifiable-agent harness build, targeting the Step 4 portability proof (proof.py + DeterministicFingerprintDriver) as the self-discredited linchpin. Citations: arXiv:2509.11068 "Tractable Asymmetric Verification" (cross-engine determinism is hardware-fragile — replicated on RTX 4000 Ada, fails on A40 from FP discrepancies) and arXiv:2607.15684 "Agent-Reactive Bugs" (statement fabrication ≈90% of cases are SILENT errors — the harness's PASS/FAIL + audit-record gates catch contract-shape violations but are silent-error blind; Codex #6562: agent claims steps it never performed, audit record looks honest). Also flagged arXiv:2602.22302 AgentContract-Bench/ABC as direct prior art the build shouldn't reinvent (Conformance Tier B hard/soft + drift). REQUIRED CLOSE: abandon the portability proof as linchpin; replace with (i) a real second-execution-path cross-check and (ii) a silent-error/fabrication gate reconciling the audit record's claimed tool calls against the actual tool-call log. The view explicitly could NOT open the agpack source; the target/reconstructions lean on FOCUS.md + the agpack thread's 08-30 caveat, so any claim about agpack internals depends on that record, not a source it verified. (source: /home/iconbaypark2900/OpenWorker/knowledge/report-2026-09-02.md)
- 2026-08-30 — Step 4 (Portability Proof) COMPLETED 2026-08-31: suite now 231 passed (up from 203). Added portability/driver.py (RuntimeDriver/Instance protocols, ToolCall/RunResult, sha256_prefix16, DFD# envelope, port()), portability/drivers.py (PurePythonDriver + DeterministicFingerprintDriver — both execute identical module bytes via isolated-namespace exec), portability/proof.py (run_portability_proof + assert_equivalent, tolerance only for envelope diffs, fixture tool sum/filter-even/hex), __init__ rewritten; dead stubs profiles.py + compat_harness.py removed; 28 tests in tests/test_portability_proof.py. Suite command python -m pytest tests/ -q (pyproject pythonpath=["src"]).
- 2026-08-30 — Honest caveat 2026-08-31: agpack Step 4 "portability proof" is NOT a true cross-engine test. This host has no wasmtime/wasmer. DeterministicFingerprintDriver shares the same pure-Python exec path as PurePythonDriver — it only re-wraps output in a DFD#<ts>:<sha256_prefix16> envelope. So the proof validates the CONTRACT (same bundle bytes → same computed value, envelope allowed to differ, divergence caught) but NOT real cross-engine behavior. A real WASM/container driver can be dropped into RuntimeDriver protocol later without touching the proof, but that substitution isn't exercised today.
- 2026-08-30 — Step 5 (Metered Access) COMPLETED & COMMITTED 2026-08-31 (commit 60aaaf1): agpack/tools/metered.py (MeteredTool, Fuel, CostPolicy, MeterLedger, MeteredBroker, InsufficientFuel) + tests/test_metered_access.py. Full suite now 276 passed (up from 231). All tests GREEN, no regressions. This was the FINAL build step — agpack is now fully built (Steps 1-5 all done). Note: the MeteredBroker models billing/accounting in-process; honest-caveat tests + module docstring state it is NOT a real pay-per-call gateway/network broker/sandbox engine. Session also fixed 3 test-only bugs in test_metered_access.py (flaky-tool RuntimeError handling, wrong 6==remaining assertion, unguarded .call("z")).
- 2026-08-30 — agpack trust-module API contract (verified by reading source, 2026-08-30). Used by call_chain_demo.py + call_chain_demo_test.py in openworker-tasks/8cc21757-c15: delegation.py `verify(token, *, logical_now_unix, keys)` runs 8 fail-fast checks in fixed order: types→scope→resource-ns→ownership→chain-line→per-hop-sig→expiry(strict, expires_at_unix>logical_now)→budget-monotone (per meter fuel_max/memory_pages_max/wall_time_ms, each child<=parent). `ChainHop` frozen dataclass (no .replace()—use stdlib dataclasses.replace); `DelegationToken` fields v/token_id/iss/sub/scope/resource/budget/expires_at_unix/chain. Resource is closed-namespace mem.<id>.<field>, owned by sub. scope must equal sub. signing: `sign(msg, seed_32_bytes)->SignatureBlock` (`.signature_bytes`); verify() wants keys: agent_id->32 raw Ed25519 pubkey bytes (not SignatureBlock). audit: `replay(ledger, kind)` -> list[dict{ordinal,run_id,kind,ts_unix,subject,detail}] (pure, strict schema, raises LedgerCorrupt). KIND_DELEGATE detail schema: token_id(str)/parent_token_id(str|None)/hop_depth(int)/scope(str)/resource(str) — hop_depth must be a real int. capabilities.Scope: enum members are MEMORY_GET/MEMORY_SET/FS_READ/FS_WRITE/NET_FETCH/CLOCK_NOW/RANDOM/EMIT_TEXT (NOT MEM_READ/WR/SET). NOTE: agpack source is read-only at /home/iconbaypark2900/dataScience/agpack/.scratch/agpack; conftest.py puts it on sys.path for the demo/tests. (source: /home/iconbaypark2900/openworker-tasks/8cc21757-c15/call_chain_demo.py)
